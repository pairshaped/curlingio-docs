<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://curling.io/blog</id>
    <title>Curling IO Dev Blog</title>
    <updated>2026-03-12T00:00:00.000Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://curling.io/blog"/>
    <subtitle>Developer insights on building and enhancing Curling IO</subtitle>
    <icon>https://curling.io/img/favicon.ico</icon>
    <rights>Copyright © 2026 Curling IO</rights>
    <entry>
        <title type="html"><![CDATA[AI Agents Love Gleam]]></title>
        <id>https://curling.io/blog/21-reasons-ai-agents-love-gleam</id>
        <link href="https://curling.io/blog/21-reasons-ai-agents-love-gleam"/>
        <updated>2026-03-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Fair warning: this post contains some opinions that are going to be controversial and may not age well. Here be dragons.]]></summary>
        <content type="html"><![CDATA[<p><em>Fair warning: this post contains some opinions that are going to be controversial and may not age well. Here be dragons.</em></p>
<p>AI coding agents like Claude Code, OpenAI Codex, and Google Gemini can write code, run it, read the errors, and try again. That loop is the whole game. The faster and more informative that loop is, the more useful the agent becomes. After building Curling IO Version 3 in Gleam alongside AI coding agents, I'm convinced Gleam is the best language for this workflow. Agents don't write better Gleam - there's less training data. But Gleam's compiler lets agents self-correct without waiting for a human.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-feedback-loop-that-matters">The Feedback Loop That Matters<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#the-feedback-loop-that-matters" class="hash-link" aria-label="Direct link to The Feedback Loop That Matters" title="Direct link to The Feedback Loop That Matters">​</a></h2>
<p>Every AI coding agent works the same way: write code, check if it works, fix what's broken, repeat. The quality of that "check if it works" step determines everything.</p>
<p>In a dynamically typed language, "check if it works" means running the test suite. Tests take time, they might not cover the thing that's actually broken, and they're code too - every test you add increases the complexity of your project. And many bugs don't surface until runtime, sometimes much later, in production. The agent writes code that looks correct, you review it, it looks correct to you too, and then a user hits a nil error at 2am.</p>
<p>In Gleam, "check if it works" means compiling. That takes a few seconds. When compilation fails, the error messages are specific: here's the file, here's the line, here's what you wrote, here's what was expected. The agent reads that, fixes it, and compiles again. A few rounds of this and the code is structurally sound.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="what-the-compiler-catches">What the Compiler Catches<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#what-the-compiler-catches" class="hash-link" aria-label="Direct link to What the Compiler Catches" title="Direct link to What the Compiler Catches">​</a></h2>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="no-nulls">No Nulls<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#no-nulls" class="hash-link" aria-label="Direct link to No Nulls" title="Direct link to No Nulls">​</a></h3>
<p>Gleam has no null. Optional values are represented as <code>Option(T)</code>, which is either <code>Some(value)</code> or <code>None</code>. You can't accidentally dereference a nil. The compiler forces you to handle both cases. Null-related errors are <a href="https://www.harness.io/blog/10-exception-types-in-production-java-applications" target="_blank" rel="noopener noreferrer">the most common exception in production</a>, appearing in 70% of production environments in a study of over 1 billion events.</p>
<p>When an agent writes code in JavaScript, it has to remember to check for null everywhere. It doesn't always remember. Gleam removes the possibility entirely.</p>
<p>Null references are famously the <a href="https://www.infoq.com/presentations/Null-References-The-Billion-Dollar-Mistake-Tony-Hoare/" target="_blank" rel="noopener noreferrer">billion dollar mistake</a>, and they're not just a theoretical concern. In June 2025, <a href="https://dev.to/pantoai/how-a-null-pointer-exception-brought-down-mighty-google-7-hours-of-downtime-explained-p2g" target="_blank" rel="noopener noreferrer">a single null value in a database field</a> cascaded through Google Cloud's Service Control system and took down multiple GCP and Workspace products worldwide for hours. The null hit a code path with no error handling, replicated globally in seconds, and the resulting outage took nearly three hours to fully resolve. Gleam's <code>Option</code> type would have forced the developer or the agent to handle the missing case before the code compiled. It doesn't prevent every failure in a chain like that, but it removes the specific class of bug that triggered it.</p>
<p>There's a security angle too. Unhandled nulls can leave an application in unexpected states - authentication checks skipped because a nil slipped through, or data exposed through an error page that should never have been reached. Every error class the compiler eliminates is attack surface the agent can't accidentally introduce.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="structural-changes">Structural Changes<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#structural-changes" class="hash-link" aria-label="Direct link to Structural Changes" title="Direct link to Structural Changes">​</a></h3>
<p>Here's a real example from our codebase. Say the agent adds a new field to a type:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub type Listing {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  Listing(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    id: Int,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    name: String,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    capacity: Int,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    waitlist_enabled: Bool,  // new field</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  )</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>In JavaScript, adding a property to an object changes nothing until runtime. If some template or API endpoint doesn't include the new field, you won't know until that code path executes. Your test suite might catch it. Or it might not, if coverage is incomplete.</p>
<p>In Gleam, every function that constructs or destructures a <code>Listing</code> now fails to compile. The compiler lists every location that needs updating. The agent works through the list, updates each one, and compiles clean.</p>
<p>This extends to every structural change: renaming a field, changing a type from <code>String</code> to <code>Int</code>, adding a variant to a union type. Agents handle this well. Humans forget things in lists.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="exhaustive-pattern-matching">Exhaustive Pattern Matching<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#exhaustive-pattern-matching" class="hash-link" aria-label="Direct link to Exhaustive Pattern Matching" title="Direct link to Exhaustive Pattern Matching">​</a></h3>
<p>Gleam's compiler requires that pattern matches cover every possible case. If you match on a <code>Result</code> type, you handle both <code>Ok</code> and <code>Error</code>. If you match on a custom union type with four variants, you handle all four. Miss one and the compiler tells you.</p>
<p>Say you have a payment status type:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub type PaymentStatus {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  Pending</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  Completed</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  Refunded</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  Failed</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>If someone adds a <code>Disputed</code> variant, every <code>case</code> expression matching on <code>PaymentStatus</code> across the entire codebase will fail to compile until it handles <code>Disputed</code>. In a dynamically typed language, the agent writes a <code>switch</code> or <code>if</code> chain that handles the common cases and forgets the edge case. That's a runtime error waiting to happen. In Gleam, the compiler catches it before the code ever runs.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="simple-syntax-fewer-ways-to-go-wrong">Simple Syntax, Fewer Ways to Go Wrong<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#simple-syntax-fewer-ways-to-go-wrong" class="hash-link" aria-label="Direct link to Simple Syntax, Fewer Ways to Go Wrong" title="Direct link to Simple Syntax, Fewer Ways to Go Wrong">​</a></h2>
<p>Gleam is a small language. There's one way to define a function, one way to handle errors and optional values (<code>Result</code> and <code>Option</code>). No exceptions, no implicit conversions, no macros.</p>
<p>This matters for agents because smaller decision space means fewer wrong decisions. When there are six ways to do something, the agent has to pick one, and it might not pick the idiomatic one. In Gleam, there's usually one way. The agent doesn't need to know the community's style preferences or the codebase's conventions for error handling. The language already decided.</p>
<p>Formatting is the same story. <code>gleam format</code> is canonical. No configuration, no style debates. The agent's output looks identical to hand-written code after formatting. You can't tell the difference, and you don't need to.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="whitespace-doesnt-matter">Whitespace Doesn't Matter<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#whitespace-doesnt-matter" class="hash-link" aria-label="Direct link to Whitespace Doesn't Matter" title="Direct link to Whitespace Doesn't Matter">​</a></h2>
<p>I actually prefer whitespace-significant languages. Elm and Haskell are two of my favorites. Clean indentation instead of curly braces everywhere looks better to me. But agents aren't good at it yet. In my experience, agents constantly trip over Slim templates: indentation errors are common with LLM-generated code. The model might mix tabs and spaces, or get the nesting level wrong by one indent. These errors are silent and semantic (they change what the code does).</p>
<p>Gleam uses curly braces. Whitespace is irrelevant to the compiler. <code>gleam format</code> normalizes it. One fewer class of errors for agents to make.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="tight-feedback-loops">Tight Feedback Loops<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#tight-feedback-loops" class="hash-link" aria-label="Direct link to Tight Feedback Loops" title="Direct link to Tight Feedback Loops">​</a></h2>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="fast-compilation">Fast Compilation<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#fast-compilation" class="hash-link" aria-label="Direct link to Fast Compilation" title="Direct link to Fast Compilation">​</a></h3>
<p>Gleam's compiler runs in a few seconds for our full project. A typical test suite in a dynamically typed language takes 30 seconds to a few minutes. When the agent is iterating, the difference between a few-second compile and a multi-minute test run adds up fast.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="fast-tests">Fast Tests<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#fast-tests" class="hash-link" aria-label="Direct link to Fast Tests" title="Direct link to Fast Tests">​</a></h3>
<p>Tests are fast too. Because each test gets its own in-memory SQLite database with no shared state, we <a href="https://curling.io/blog/parallel-tests-for-free">run them all in parallel</a>. Around 800 tests finish in under a second. That speed accumulates over a session where the agent is compiling and testing dozens of times.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="less-training-data-is-a-real-cost">Less Training Data Is a Real Cost<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#less-training-data-is-a-real-cost" class="hash-link" aria-label="Direct link to Less Training Data Is a Real Cost" title="Direct link to Less Training Data Is a Real Cost">​</a></h2>
<p>Gleam has less training data than JavaScript, TypeScript, or Python. Agents write worse Gleam on their first attempt. They reach for patterns that don't exist and invent functions that aren't in the standard library.</p>
<p>This is a real cost. The agent takes longer to write initial code in Gleam than it would in JavaScript.</p>
<p>But here's what I've found: the total time from "start writing" to "code is correct and deployed" is shorter in Gleam. The agent writes slower but the compiler catches errors instantly. In JavaScript, the agent writes faster but errors surface later, in tests or in production.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-workflow-in-practice">The Workflow in Practice<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#the-workflow-in-practice" class="hash-link" aria-label="Direct link to The Workflow in Practice" title="Direct link to The Workflow in Practice">​</a></h2>
<p>Here's how a typical feature goes:</p>
<ol>
<li>We write a spec describing what we want. This is iterative - we go back and forth with the agent refining the spec before any code is written. A good spec is the difference between useful output and garbage.</li>
<li>The agent writes the implementation: types, database queries, request handlers, views</li>
<li>It runs <code>gleam build</code>. Compilation fails with 5-10 errors.</li>
<li>The agent reads each error, fixes the code, rebuilds. A few rounds.</li>
<li>Compilation succeeds. The agent runs the tests. They pass, or they fail on business logic (not on null errors or type mismatches).</li>
<li>We review the diff. We're looking at logic, intent, and redundancy.</li>
</ol>
<p>Agents tend to duplicate code rather than reuse existing functions, probably because of context window limits. Catching those opportunities to extract shared logic is the most common feedback we give.</p>
<p>We still write a fair amount of code by hand. Agents aren't always right, and they sometimes produce ugly or redundant code that needs to be caught and rewritten. We step in when the agent is spinning out on something, going in circles trying to fix the same error. In Gleam that happens a lot less than what we've seen in dynamic languages.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="other-languages">Other Languages<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#other-languages" class="hash-link" aria-label="Direct link to Other Languages" title="Direct link to Other Languages">​</a></h2>
<p>Other statically typed languages with good compilers share some of these properties. Rust, OCaml, Haskell, and Elm all have strong type systems that catch errors at compile time.</p>
<p>But Gleam has a specific combination that makes it well-suited for agents:</p>
<ul>
<li>Simple syntax that's easy to generate</li>
<li>Fast compilation for tight feedback loops</li>
<li>Canonical formatting so agent output looks like hand-written code</li>
<li>No nulls, eliminating one of the most common error classes</li>
<li>Exhaustive pattern matching so no cases are forgotten</li>
<li>Whitespace-insensitive so formatting errors can't change behavior</li>
</ul>
<p>It also runs on the BEAM, which gives you fault tolerance and concurrency, but that's a <a href="https://curling.io/blog/background-jobs-without-the-baggage">separate conversation</a>.</p>
<p>The trade-off is ecosystem maturity and training data. Gleam is young. Libraries are fewer. Agent-generated code needs more correction on the first pass. That gap is closing as training data grows.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-bet">The Bet<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#the-bet" class="hash-link" aria-label="Direct link to The Bet" title="Direct link to The Bet">​</a></h2>
<p>I believe AI agents are already writing most of the code in a growing number of projects. Language choice is being evaluated differently. "How fast can a human write this?" matters less. "How much of the developer's review time does this require?" matters most.</p>
<p>The developer is the bottleneck. We're slow compared to computers. A language where the compiler has already verified structural correctness before the diff reaches your screen means the reviewer can focus on logic and intent instead of chasing down missing nil checks.</p>
<p>We picked Gleam for other reasons, but how well it works with AI coding agents has been huge.</p>
<hr>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="addendum-the-state-of-ai-assisted-coding">Addendum: The State of AI-Assisted Coding<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#addendum-the-state-of-ai-assisted-coding" class="hash-link" aria-label="Direct link to Addendum: The State of AI-Assisted Coding" title="Direct link to Addendum: The State of AI-Assisted Coding">​</a></h2>
<p>Stepping back from the technical argument. AI-assisted coding is here to stay. Even if the technology doesn't meaningfully improve from where it is today, it's already useful enough that developers are adopting it en masse.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="the-bar-is-higher-not-lower">The Bar Is Higher, Not Lower<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#the-bar-is-higher-not-lower" class="hash-link" aria-label="Direct link to The Bar Is Higher, Not Lower" title="Direct link to The Bar Is Higher, Not Lower">​</a></h3>
<p>Agents are good enough for a lot of business application work right now, but "good enough" comes with a big asterisk. Every diff needs scrutiny. The developer needs to be a domain expert, a security expert, and a programming expert. The speed at which code is produced has raised the expertise required from the developer, not lowered it.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="expect-more-security-vulnerabilities">Expect More Security Vulnerabilities<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#expect-more-security-vulnerabilities" class="hash-link" aria-label="Direct link to Expect More Security Vulnerabilities" title="Direct link to Expect More Security Vulnerabilities">​</a></h3>
<p>Agents generate code that works, but "works" and "secure" are very different bars. They'll probably catch the obvious stuff like SQL injection. The less obvious stuff is where it gets dangerous - like not realizing that a decision you made three prompts ago means all your customers' PII is now accessible on a public URL as a side effect. That requires understanding the full picture, and agents don't have that yet. I think this will become a visible reality over the next few years as more agent-written code hits production without adequate review.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="the-industry-is-evolving">The Industry Is Evolving<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#the-industry-is-evolving" class="hash-link" aria-label="Direct link to The Industry Is Evolving" title="Direct link to The Industry Is Evolving">​</a></h3>
<p>The industry is actively trying to figure out what the right abstractions look like for AI-assisted development. Two notable attempts:</p>
<p><a href="https://moglang.org/" target="_blank" rel="noopener noreferrer">Mog</a> is a language designed to be written and read by AIs rather than humans. I'm skeptical. If the developer reviewing the diff is the bottleneck, optimizing for machine readability at the expense of human readability makes things worse.</p>
<p><a href="https://codespeak.dev/" target="_blank" rel="noopener noreferrer">Codespeak</a> takes a different approach: specs and code as interchangeable representations, where you can move between them seamlessly. I think both specs and code are important, and right now neither is enough on its own. A spec is great for big-picture thinking, but it lacks the detail that matters when things go wrong. Code has all the detail, but it's hard to step back and reason about the whole system by reading it. They operate at different levels of granularity, and collapsing them into one thing loses what makes each useful.</p>
<p>Statically typed languages have an advantage here: types are self-documenting. A well-defined type in Gleam already communicates a lot of what a spec would say about the shape of data and the boundaries of a function, without needing a separate document to describe it.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="what-comes-next">What Comes Next<a href="https://curling.io/blog/21-reasons-ai-agents-love-gleam#what-comes-next" class="hash-link" aria-label="Direct link to What Comes Next" title="Direct link to What Comes Next">​</a></h3>
<p>The developer's role is shifting toward a supervisory one. That might mean a lot more software gets written, or a lot fewer developers get employed, or both. And the supervisory role itself might not last if agents get good enough to close the loop on their own. That's a real possibility and it's worth being honest about.</p>
<p>After a year of building production software with AI coding agents, it works if you put in the effort to verify everything that comes out the other end. Gleam wasn't designed for AI agents, but good language design turns out to matter more than any tool built specifically for them.</p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="gleam" term="gleam"/>
        <category label="ai" term="ai"/>
        <category label="architecture" term="architecture"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[Parallel Tests for Free]]></title>
        <id>https://curling.io/blog/parallel-tests-for-free</id>
        <link href="https://curling.io/blog/parallel-tests-for-free"/>
        <updated>2026-03-08T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[While writing the previous post about our per-test SQLite databases, I was describing how each test gets its own in-memory database, no shared connections, no shared state. And I thought: wait, if nothing is shared, can we just run them all at the same time?]]></summary>
        <content type="html"><![CDATA[<p>While writing the <a href="https://curling.io/blog/sqlite-test-isolation">previous post</a> about our per-test SQLite databases, I was describing how each test gets its own in-memory database, no shared connections, no shared state. And I thought: wait, if nothing is shared, can we just run them all at the same time?</p>
<p>Turns out we could, and our server test suite went from ~4 seconds to ~0.85 seconds for around 800 tests. Zero code changes to the tests themselves. One 25-line Erlang module.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-sequential-was-the-default">Why Sequential Was the Default<a href="https://curling.io/blog/parallel-tests-for-free#why-sequential-was-the-default" class="hash-link" aria-label="Direct link to Why Sequential Was the Default" title="Direct link to Why Sequential Was the Default">​</a></h2>
<p>Gleam's test runner, gleeunit, delegates to Erlang's EUnit framework. By default, EUnit runs test modules one at a time. This is the safe choice because most test suites have shared mutable state somewhere: a database connection, a named process, a file on disk. Running those tests concurrently produces the kind of failures that pass locally and fail in CI, or pass on Tuesday and fail on Wednesday.</p>
<p>Our tests don't have shared mutable state. Every test clones its own database. No two tests touch the same connection. The previous post explains the full setup, but the short version is: <code>test_db.setup()</code> clones a cached template database via SQLite's backup API and returns a fresh, independent connection. When the test ends, the connection is garbage collected and the database disappears.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="whats-actually-isolated">What's Actually Isolated<a href="https://curling.io/blog/parallel-tests-for-free#whats-actually-isolated" class="hash-link" aria-label="Direct link to What's Actually Isolated" title="Direct link to What's Actually Isolated">​</a></h2>
<p>Before flipping the switch, we checked every category of shared state:</p>
<table><thead><tr><th>Resource</th><th>Isolation</th></tr></thead><tbody><tr><td>SQLite databases</td><td>Each test clones its own in-memory DB</td></tr><tr><td><code>persistent_term</code> cache</td><td>Idempotent first-writer-wins (template DB, timezone data, logger config)</td></tr><tr><td>ETS tables (rate limiter)</td><td>Unnamed, each test creates its own via <code>ets:new</code></td></tr><tr><td>Named processes</td><td>None started in tests</td></tr><tr><td>File system</td><td>No writes in tests</td></tr></tbody></table>
<p>The <code>persistent_term</code> entries are write-once caches. Multiple tests might try to initialize the template database at the same time, but the first one wins and subsequent calls just read the cached value. That's safe.</p>
<p>ETS tables used in tests (for the rate limiter) are created without the <code>named_table</code> option, so each call to <code>ets:new</code> returns a unique table reference. No conflicts.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-implementation">The Implementation<a href="https://curling.io/blog/parallel-tests-for-free#the-implementation" class="hash-link" aria-label="Direct link to The Implementation" title="Direct link to The Implementation">​</a></h2>
<p>EUnit supports a <code>{inparallel, Tests}</code> wrapper that distributes test functions across BEAM schedulers. It's been there for years, but gleeunit doesn't expose it. We submitted a PR to add a <code>main_parallel()</code> function, but it was pointed out that this fits better as a project-local solution than a change to gleeunit's core. That makes sense. We closed the PR and wrote a project-local Erlang module that replicates the test discovery logic and calls EUnit directly.</p>
<div class="language-erlang codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-erlang codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">-module(parallel_test_runner).</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">-export([main/0]).</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">main() -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    Options = [verbose, no_tty,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">               {report, {gleeunit_progress, [{colored, true}]}},</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">               {scale_timeouts, 10}],</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    Files = filelib:wildcard("**/*.{erl,gleam}", "test"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    Modules = [to_module(list_to_binary(F)) || F &lt;- Files],</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    case eunit:test({inparallel, Modules}, Options) of</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        ok -&gt; erlang:halt(0);</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        _  -&gt; erlang:halt(1)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    end.</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">to_module(Path) -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    case filename:extension(Path) of</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        &lt;&lt;".gleam"&gt;&gt; -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">            binary_to_atom(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">              binary:replace(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">                binary:replace(Path, &lt;&lt;".gleam"&gt;&gt;, &lt;&lt;""&gt;&gt;),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">                &lt;&lt;"/"&gt;&gt;, &lt;&lt;"@"&gt;&gt;, [global]),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">              utf8);</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        &lt;&lt;".erl"&gt;&gt; -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">            binary_to_atom(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">              binary:replace(lists:last(binary:split(Path, &lt;&lt;"/"&gt;&gt;, [global])),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">                &lt;&lt;".erl"&gt;&gt;, &lt;&lt;""&gt;&gt;),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">              utf8)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    end.</span><br></span></code></pre></div></div>
<p>This does the same thing gleeunit does (glob for test files, convert filenames to module atoms) but wraps the module list in <code>{inparallel, ...}</code> instead of passing it flat. It reuses <code>gleeunit_progress</code> from the hex dependency for the dot-per-test output with color, so the test output looks the same as before.</p>
<p>The test entry point is one line:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">@external(erlang, "parallel_test_runner", "main")</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn main() -&gt; Nil</span><br></span></code></pre></div></div>
<p>No fork. No submodule. Stock gleeunit stays in <code>gleam.toml</code> as a dependency (we still use it for the progress reporter). Only the server package uses the parallel runner. The shared and client packages use gleeunit's standard sequential <code>main()</code> because they have far fewer tests and don't need it.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-results">The Results<a href="https://curling.io/blog/parallel-tests-for-free#the-results" class="hash-link" aria-label="Direct link to The Results" title="Direct link to The Results">​</a></h2>
<table><thead><tr><th>Package</th><th>Tests</th><th>Before</th><th>After</th></tr></thead><tbody><tr><td>server</td><td>~800</td><td>~4.0s</td><td>~0.85s</td></tr><tr><td>shared</td><td>~100</td><td>~0.5s</td><td>~0.5s</td></tr><tr><td>client</td><td>1</td><td>~0.2s</td><td>~0.2s</td></tr><tr><td><strong>Total</strong></td><td><strong>~900</strong></td><td><strong>~4.7s</strong></td><td><strong>~1.6s</strong></td></tr></tbody></table>
<p>The shared and client packages didn't change because they already ran in under a second.</p>
<p>EUnit serializes its listener callbacks through a single process, so the dot-per-test progress output still arrives in order. Per-test stdout is delivered after each test completes, so you don't get interleaved output from concurrent tests. The test output looks identical to before, just faster.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="keeping-it-working">Keeping It Working<a href="https://curling.io/blog/parallel-tests-for-free#keeping-it-working" class="hash-link" aria-label="Direct link to Keeping It Working" title="Direct link to Keeping It Working">​</a></h2>
<p>This works because the test architecture is clean. If someone adds a test that registers a named process with a fixed atom, or writes to a shared file path, two copies of that test will collide and produce flaky failures. The rules are simple:</p>
<ul>
<li>Don't share database connections between tests. Use <code>test_db.setup()</code>.</li>
<li>Don't register processes with fixed names. If you need a named process, include the test module name.</li>
<li>Don't use named ETS tables. Pass the table reference instead.</li>
<li>Don't write to the file system in tests.</li>
</ul>
<p>If a test can't follow these rules, EUnit lets you mix <code>{inparallel, ...}</code> and <code>{inorder, ...}</code> in the same test run. We could maintain a list of sequential modules in the runner and group them separately:</p>
<div class="language-erlang codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-erlang codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">Sequential = [some_integration_test],</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">Parallel = Modules -- Sequential,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">eunit:test([{inparallel, Parallel}, {inorder, Sequential}], Options)</span><br></span></code></pre></div></div>
<p>The sequential modules would run one at a time while everything else stays parallel. We haven't needed this yet, but it's a few lines if we do.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-accidental-win">The Accidental Win<a href="https://curling.io/blog/parallel-tests-for-free#the-accidental-win" class="hash-link" aria-label="Direct link to The Accidental Win" title="Direct link to The Accidental Win">​</a></h2>
<p>None of this was planned. We chose per-test SQLite databases because they eliminated cleanup code, ordering dependencies, and flaky tests from leaked state. We're still a bit surprised that a permanent 4.7x speedup, one we'll benefit from for the lifetime of this project, took an afternoon and 25 lines of Erlang.</p>
<p>37 lines total (counting the 12-line FFI module from the previous post) for per-test database isolation and parallel execution across all available CPU cores. No test framework plugins. No configuration. The whole thing compiles with <code>gleam build</code> and runs with <code>gleam test</code>.</p>
<hr>
<p><em>This is Part 7 of the Curling IO Foundation series.</em></p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="foundation" term="foundation"/>
        <category label="gleam" term="gleam"/>
        <category label="beam" term="beam"/>
        <category label="testing" term="testing"/>
        <category label="architecture" term="architecture"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[Every Provincial and Territorial Curling Association in Canada Uses Curling IO]]></title>
        <id>https://curling.io/blog/curling-io-powers-provincial-associations</id>
        <link href="https://curling.io/blog/curling-io-powers-provincial-associations"/>
        <updated>2026-03-06T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[All 14 provincial and territorial curling associations, plus Curling Canada, run on Curling IO. Here is what that means for clubs and curlers.]]></summary>
        <content type="html"><![CDATA[<p>Every provincial and territorial curling association in Canada uses Curling IO, and so does the national body, Curling Canada. That's 15 membership associations, all on the same platform, along with their affiliated clubs.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-full-list">The Full List<a href="https://curling.io/blog/curling-io-powers-provincial-associations#the-full-list" class="hash-link" aria-label="Direct link to The Full List" title="Direct link to The Full List">​</a></h2>
<ul>
<li>
<a href="https://canada.curling.io/" target="_blank">Curling Canada</a>
</li>
<li>
<a href="https://ab.curling.io/" target="_blank">Curling Alberta</a>
</li>
<li>
<a href="https://bc.curling.io/" target="_blank">Curl BC</a>
</li>
<li>
<a href="https://mb.curling.io/" target="_blank">CurlManitoba</a>
</li>
<li>
<a href="https://sk.curling.io/" target="_blank">CURLSASK</a>
</li>
<li>
<a href="https://on.curling.io/" target="_blank">Curling Ontario</a>
</li>
<li>
<a href="https://noca.curling.io/" target="_blank">Northern Ontario Curling Association</a>
</li>
<li>
<a href="https://qc.curling.io/" target="_blank">Curling Québec</a>
</li>
<li>
<a href="https://nb.curling.io/" target="_blank">New Brunswick Curling Association</a>
</li>
<li>
<a href="https://ns.curling.io/" target="_blank">Nova Scotia Curling</a>
</li>
<li>
<a href="https://nl.curling.io/" target="_blank">Newfoundland &amp; Labrador Curling Association</a>
</li>
<li>
<a href="https://pe.curling.io/" target="_blank">Curl PEI</a>
</li>
<li>
<a href="https://nt.curling.io/" target="_blank">Curling NT</a>
</li>
<li>
<a href="https://nu.curling.io/" target="_blank">Nunavut Curling Association</a>
</li>
<li>
<a href="https://yt.curling.io/" target="_blank">Yukon Curling Association</a>
</li>
</ul>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="what-associations-use-it-for">What Associations Use It For<a href="https://curling.io/blog/curling-io-powers-provincial-associations#what-associations-use-it-for" class="hash-link" aria-label="Direct link to What Associations Use It For" title="Direct link to What Associations Use It For">​</a></h2>
<p>Provincial associations use Curling IO to run their competitions: provincial championships, playdowns, regional qualifiers. They manage team registrations, schedule draws, score games live, and publish results. Fans and participants see brackets, standings, and scoreboards update in real time. For a closer look at how this works at the national level, see <a href="https://curling.io/blog/how-curling-canada-uses-curling-io">how Curling Canada uses Curling IO</a>.</p>
<p>Associations also collect affiliate fees through the platform. When a curler registers at their local club, the club's registration fees and the association's affiliate fees are collected together in one transaction. Curling Canada's national fee, the provincial fee, and the club's own fees all appear on a single checkout. No separate invoicing, no manual reconciliation.</p>
<p>The affiliate fee tie-in is optional. Clubs can use Curling IO independently without inheriting association fees or reporting member data. But clubs that do opt in get the fee collection and reporting handled automatically.</p>
<p>The platform is fully bilingual (English and French), which matters for associations like Curling Québec and for clubs across the country with francophone members.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-it-matters-for-clubs">Why It Matters for Clubs<a href="https://curling.io/blog/curling-io-powers-provincial-associations#why-it-matters-for-clubs" class="hash-link" aria-label="Direct link to Why It Matters for Clubs" title="Direct link to Why It Matters for Clubs">​</a></h2>
<p>When your provincial association is on Curling IO, a few things work in your favour.</p>
<p><strong>Your curlers already have profiles.</strong> A curler who registered for a provincial bonspiel or played in a championship already exists in the system. When they register at your club, they find their existing profile. No re-entering names, addresses, or emergency contacts.</p>
<p><strong>Competitions connect.</strong> A curler's results from club leagues, provincial playdowns, and national championships all live in the same system. Their profile carries from club leagues to provincial to national competitions.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-it-matters-for-curlers">Why It Matters for Curlers<a href="https://curling.io/blog/curling-io-powers-provincial-associations#why-it-matters-for-curlers" class="hash-link" aria-label="Direct link to Why It Matters for Curlers" title="Direct link to Why It Matters for Curlers">​</a></h2>
<p>From a curler's perspective, Curling IO is one account. They register for their Tuesday night league at their local club, sign up for a weekend bonspiel at another club, and qualify for a provincial championship. Same login, same profile, same payment flow.</p>
<p>There are currently around 150,000 curler profiles in the system across hundreds of active clubs and all 15 membership associations. That number grows every season as more clubs come online.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="getting-started">Getting Started<a href="https://curling.io/blog/curling-io-powers-provincial-associations#getting-started" class="hash-link" aria-label="Direct link to Getting Started" title="Direct link to Getting Started">​</a></h2>
<p>If your club is under a Canadian provincial association, your association is already on Curling IO. Getting started means your affiliate fees and curler database are connected from day one. There's no setup fee, no monthly fee, and no contract.</p>
<p>Clubs outside of Canada can use Curling IO too, just without the association tie-in.</p>
<p>Check out our <a href="https://curling.io/docs/getting-started/curling-club-managers">getting started guide</a> or visit your provincial association's Curling IO page to see it in action.</p>]]></content>
        <author>
            <name>Chris</name>
        </author>
        <category label="provincial-associations" term="provincial-associations"/>
        <category label="partnerships" term="partnerships"/>
        <category label="curler-database" term="curler-database"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[Test Isolation for Free with SQLite]]></title>
        <id>https://curling.io/blog/sqlite-test-isolation</id>
        <link href="https://curling.io/blog/sqlite-test-isolation"/>
        <updated>2026-03-03T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Most web frameworks treat test database isolation as a hard problem. Rails has database_cleaner with three strategies. Django wraps every test in a transaction it rolls back. Phoenix does the same with its SQL sandbox. They all exist because tests share a single database server, and that shared state is the root of flaky tests and ordering dependencies, the kind where a test passes alone but fails in the suite.]]></summary>
        <content type="html"><![CDATA[<p>Most web frameworks treat test database isolation as a hard problem. Rails has <code>database_cleaner</code> with three strategies. Django wraps every test in a transaction it rolls back. Phoenix does the same with its SQL sandbox. They all exist because tests share a single database server, and that shared state is the root of flaky tests and ordering dependencies, the kind where a test passes alone but fails in the suite.</p>
<p>Curling IO Version 3 doesn't have this problem. Each test gets its own database. Not a transaction. Not a truncated copy. A completely independent in-memory SQLite database, cloned from a template in microseconds using SQLite's backup API.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-shared-database-problem">The Shared Database Problem<a href="https://curling.io/blog/sqlite-test-isolation#the-shared-database-problem" class="hash-link" aria-label="Direct link to The Shared Database Problem" title="Direct link to The Shared Database Problem">​</a></h2>
<p>In a traditional Postgres or MySQL test setup, every test talks to the same database server. You need a strategy to keep tests from contaminating each other:</p>
<p><strong>Transaction rollback.</strong> Wrap each test in a transaction, roll it back at the end. Works until your code uses transactions internally, or spawns processes that need to see the test data (the classic Ecto sandbox problem).</p>
<p><strong>Truncation.</strong> Delete all rows from every table between tests. Slow, and you need to get the table ordering right to avoid foreign key violations.</p>
<p><strong>Database cleaner.</strong> A gem/library that combines both strategies with configuration for which tables to clean, which strategy to use, and when. It works, but it's ceremony that exists purely because of the shared database.</p>
<p>All of these are workarounds for the same architectural constraint: one database server, many tests.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="in-memory-sqlite-one-database-per-test">In-Memory SQLite: One Database Per Test<a href="https://curling.io/blog/sqlite-test-isolation#in-memory-sqlite-one-database-per-test" class="hash-link" aria-label="Direct link to In-Memory SQLite: One Database Per Test" title="Direct link to In-Memory SQLite: One Database Per Test">​</a></h2>
<p>SQLite opens a database by passing a file path. Pass <code>:memory:</code> instead and you get an in-memory database that exists only for the lifetime of that connection. It's fast (no disk I/O) and completely isolated. Nothing else can see it, and closing the connection frees everything.</p>
<p>Our test setup function:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn setup() -&gt; sqlight.Connection {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let assert Ok(conn) = sqlight.open(":memory:")</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let assert Ok(_) = sqlight.exec("PRAGMA foreign_keys=ON;", conn)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let assert Ok(_) = sqlight.exec(schema_sql, conn)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  conn</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>The <code>schema_sql</code> variable contains the contents of <code>db/schema.sql</code>, which is regenerated from migrations by <code>bin/reset</code>. This means tests always use the current schema without anyone remembering to update them.</p>
<p>Every test calls <code>setup()</code>, gets a fresh database with the full schema, inserts whatever test data it needs, and runs its assertions. When the test ends, the connection is garbage collected and the database disappears. There's nothing to clean up.</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn should_reject_duplicate_registration_test() {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let conn = test_db.setup()</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  // Insert test-specific data</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  insert_org(conn, test_org())</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  insert_listing(conn, test_listing())</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  // ... test logic, assertions</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  // conn goes out of scope, database vanishes</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>There's no possible cross-contamination because there's nothing shared. Test A and test B literally operate on different databases. You can run them in parallel on BEAM processes without any coordination.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-schema-execution-cost">The Schema Execution Cost<a href="https://curling.io/blog/sqlite-test-isolation#the-schema-execution-cost" class="hash-link" aria-label="Direct link to The Schema Execution Cost" title="Direct link to The Schema Execution Cost">​</a></h2>
<p>This approach has an obvious cost: every test executes the full schema. For us that's 77 <code>CREATE TABLE</code>, <code>CREATE INDEX</code>, and <code>CREATE VIEW</code> statements. We profiled it:</p>
<ul>
<li>Opening an in-memory SQLite database: <strong>15 microseconds</strong></li>
<li>Executing 77 schema statements: <strong>1,100 microseconds</strong> (1.1 ms)</li>
</ul>
<p>At 1.1 ms per test across ~500 tests that need a database, that's about 550 ms of schema execution. Not terrible, but not free either. And it scales linearly with both the number of tests and the size of your schema.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="sqlites-backup-api-page-level-cloning">SQLite's Backup API: Page-Level Cloning<a href="https://curling.io/blog/sqlite-test-isolation#sqlites-backup-api-page-level-cloning" class="hash-link" aria-label="Direct link to SQLite's Backup API: Page-Level Cloning" title="Direct link to SQLite's Backup API: Page-Level Cloning">​</a></h2>
<p>SQLite has a <a href="https://www.sqlite.org/backup.html" target="_blank" rel="noopener noreferrer">backup API</a> designed for copying databases between connections. It operates at the page level: it doesn't re-parse or re-execute SQL, it copies raw database pages from one connection to another.</p>
<p>The idea: build the schema once in a template database, then clone it per test.</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">Once at startup:  open(":memory:") → execute full schema → template</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">Per test:         open(":memory:") → backup_copy(template) → ready</span><br></span></code></pre></div></div>
<p>The backup API is three calls: <code>backup_init</code> (set up the copy), <code>backup_step</code> with <code>-1</code> (copy all pages in one shot), and <code>backup_finish</code> (release resources).</p>
<p>Our Gleam SQLite driver, <a href="https://hexdocs.pm/sqlight/" target="_blank" rel="noopener noreferrer">sqlight</a>, doesn't expose the backup API. But it's built on <a href="https://github.com/mmzeeman/esqlite" target="_blank" rel="noopener noreferrer">esqlite</a>, an Erlang NIF wrapper around SQLite's C library, and esqlite exposes the full backup API. Since Gleam compiles to Erlang and runs on the BEAM, we can call Erlang libraries directly through Gleam's foreign function interface (FFI). We wrote a 12-line Erlang module that takes two sqlight connections (which are just esqlite records under the hood) and performs the clone:</p>
<div class="language-erlang codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-erlang codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">-module(test_db_ffi).</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">-export([clone_db/1]).</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">clone_db(Template) -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    {ok, Dest} = esqlite3:open(":memory:"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    {ok, Backup} = esqlite3:backup_init(Dest, "main", Template, "main"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    '$done' = esqlite3:backup_step(Backup, -1),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    ok = esqlite3:backup_finish(Backup),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    {ok, Dest}.</span><br></span></code></pre></div></div>
<p>On the Gleam side, we declare the FFI binding and call it like any other function:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">@external(erlang, "test_db_ffi", "clone_db")</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">fn clone_db(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  template: sqlight.Connection,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">) -&gt; Result(sqlight.Connection, Nil)</span><br></span></code></pre></div></div>
<p>This is one of the nice things about the BEAM ecosystem. When your high-level driver doesn't expose what you need, the lower-level library almost always does, and the FFI boundary is trivial to cross.</p>
<p>The template is created once per test run and cached in BEAM's <code>persistent_term</code> (a global immutable store optimized for read-heavy access):</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">fn template_db() -&gt; sqlight.Connection {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  case get_cached("test_template_db") {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    Ok(conn) -&gt; conn</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    Error(Nil) -&gt; {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      let assert Ok(conn) = sqlight.open(":memory:")</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      let assert Ok(_) = sqlight.exec("PRAGMA foreign_keys=ON;", conn)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      let assert Ok(_) = sqlight.exec(schema_sql, conn)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      cache("test_template_db", conn)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      conn</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn setup() -&gt; sqlight.Connection {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let assert Ok(conn) = clone_db(template_db())</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let assert Ok(_) = sqlight.exec("PRAGMA foreign_keys=ON;", conn)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  conn</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>The <code>PRAGMA foreign_keys=ON</code> runs after the clone because SQLite pragma settings are per-connection, not stored in the database file. It's a single statement, negligible cost.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-numbers">The Numbers<a href="https://curling.io/blog/sqlite-test-isolation#the-numbers" class="hash-link" aria-label="Direct link to The Numbers" title="Direct link to The Numbers">​</a></h2>
<p>At the time we switched to the clone method, we had 594 tests and 77 schema objects. We're still early in the rewrite and already approaching 1,000 tests. By launch we expect closer to twice the schema objects and somewhere around 5,000 tests.</p>
<p>Here's what we measured at 594 tests:</p>
<table><thead><tr><th>Approach</th><th>Server test time</th></tr></thead><tbody><tr><td>Execute full schema per test</td><td>3.2 s</td></tr><tr><td>Clone template via backup API</td><td>2.6 s</td></tr></tbody></table>
<p>The schema execution cost dropped from ~550 ms to near zero. The remaining 2.6 seconds is actual test logic (inserting data, running business logic, assertions) plus BEAM VM startup overhead.</p>
<p>The savings become more meaningful at scale. In a traditional Postgres setup, you'd use truncation between tests, issuing <code>TRUNCATE TABLE</code> on every table, in foreign-key-safe order, over a socket to the database server. With around 100 tables and 5,000 tests (a conservative estimate for our full rewrite, realistically 2-3x the tests), that's 500,000 truncation statements hitting a database server. Even at sub-millisecond per truncate, it adds up.</p>
<p>With the backup approach, the clone cost is ~25 microseconds per test regardless of schema size. At 5,000 tests that's 0.125 seconds total. No truncation ordering, no socket round-trips.</p>
<p>For context, this is a real application test suite covering cart operations, payment processing, registration validation, affiliate fees, waivers, round robin generation, scheduling, scoring, and more. Not trivial tests.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-this-works-so-well">Why This Works So Well<a href="https://curling.io/blog/sqlite-test-isolation#why-this-works-so-well" class="hash-link" aria-label="Direct link to Why This Works So Well" title="Direct link to Why This Works So Well">​</a></h2>
<p>The backup API is a nice optimization, but the real win is that in-process SQLite eliminates the shared database problem entirely.</p>
<p>With Postgres, your test process talks to a database server over a socket. Every test that writes data is writing to the same place. You need isolation strategies because the architecture demands them.</p>
<p>With in-process SQLite, the database lives in your process's memory. Creating a new one is a memory allocation, not a network connection. There's no server to coordinate with and no connection pool to manage. The isolation falls out of the architecture for free.</p>
<p>This also means:</p>
<ul>
<li>No test ordering dependencies. Each test is fully self-contained.</li>
<li>Safe parallelism. BEAM processes each get their own database, zero coordination needed.</li>
<li>No cleanup code. No <code>teardown</code>, no <code>after_each</code>, no <code>database_cleaner</code>.</li>
<li>No flaky tests from shared state leaking between runs.</li>
</ul>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="trade-offs">Trade-offs<a href="https://curling.io/blog/sqlite-test-isolation#trade-offs" class="hash-link" aria-label="Direct link to Trade-offs" title="Direct link to Trade-offs">​</a></h2>
<p>This isn't free of trade-offs. You're not testing against the same database engine you run in production... unless you also run SQLite in production, which we do. If you're using SQLite for tests but Postgres in production, you'd miss Postgres-specific behavior (custom types, advisory locks, jsonb operators, etc.).</p>
<p>The other trade-off is that in our setup, each test builds up its own data from scratch. In practice this is a feature (every test explicitly declares its dependencies) but it does mean more setup code per test compared to a shared fixtures approach. That said, this is a choice, not a limitation of the technique. You could just as easily insert seed data into the template database before caching it, and every clone would start with that data pre-loaded. If your test suite benefits from a well-defined set of standard users, organizations, or other reference data, seeding the template is a straightforward way to reduce per-test setup while keeping full isolation.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="this-isnt-beam-specific">This Isn't BEAM-Specific<a href="https://curling.io/blog/sqlite-test-isolation#this-isnt-beam-specific" class="hash-link" aria-label="Direct link to This Isn't BEAM-Specific" title="Direct link to This Isn't BEAM-Specific">​</a></h2>
<p>Our examples are in Gleam and Erlang, but the backup API is a C-level SQLite feature. Most languages expose it:</p>
<div class="language-python codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-python codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token comment" style="color:rgb(105, 112, 152);font-style:italic"># Python 3.7+ has it built in</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">source </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> sqlite3</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token plain">connect</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token string" style="color:rgb(195, 232, 141)">":memory:"</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">source</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token plain">executescript</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token plain">schema_sql</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">dest </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> sqlite3</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token plain">connect</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token string" style="color:rgb(195, 232, 141)">":memory:"</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">source</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token plain">backup</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token plain">dest</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><br></span></code></pre></div></div>
<div class="language-javascript codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-javascript codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token comment" style="color:rgb(105, 112, 152);font-style:italic">// Node.js better-sqlite3</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain"></span><span class="token keyword" style="font-style:italic">const</span><span class="token plain"> template </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">new</span><span class="token plain"> </span><span class="token class-name" style="color:rgb(255, 203, 107)">Database</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token string" style="color:rgb(195, 232, 141)">":memory:"</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">template</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token method function property-access" style="color:rgb(130, 170, 255)">exec</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token plain">schemaSQL</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain"></span><span class="token keyword" style="font-style:italic">const</span><span class="token plain"> dest </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> template</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token method function property-access" style="color:rgb(130, 170, 255)">backup</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token string" style="color:rgb(195, 232, 141)">":memory:"</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><br></span></code></pre></div></div>
<div class="language-rust codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-rust codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token comment" style="color:rgb(105, 112, 152);font-style:italic">// Rust rusqlite</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain"></span><span class="token keyword" style="font-style:italic">let</span><span class="token plain"> template </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> </span><span class="token class-name" style="color:rgb(255, 203, 107)">Connection</span><span class="token punctuation" style="color:rgb(199, 146, 234)">::</span><span class="token function" style="color:rgb(130, 170, 255)">open_in_memory</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token operator" style="color:rgb(137, 221, 255)">?</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">template</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token function" style="color:rgb(130, 170, 255)">execute_batch</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token plain">schema_sql</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token operator" style="color:rgb(137, 221, 255)">?</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain"></span><span class="token keyword" style="font-style:italic">let</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">mut</span><span class="token plain"> dest </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> </span><span class="token class-name" style="color:rgb(255, 203, 107)">Connection</span><span class="token punctuation" style="color:rgb(199, 146, 234)">::</span><span class="token function" style="color:rgb(130, 170, 255)">open_in_memory</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token operator" style="color:rgb(137, 221, 255)">?</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain"></span><span class="token keyword" style="font-style:italic">let</span><span class="token plain"> backup </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> </span><span class="token namespace" style="color:rgb(178, 204, 214)">backup</span><span class="token namespace punctuation" style="color:rgb(199, 146, 234)">::</span><span class="token class-name" style="color:rgb(255, 203, 107)">Backup</span><span class="token punctuation" style="color:rgb(199, 146, 234)">::</span><span class="token function" style="color:rgb(130, 170, 255)">new</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token operator" style="color:rgb(137, 221, 255)">&amp;</span><span class="token plain">template</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">&amp;</span><span class="token keyword" style="font-style:italic">mut</span><span class="token plain"> dest</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token operator" style="color:rgb(137, 221, 255)">?</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">backup</span><span class="token punctuation" style="color:rgb(199, 146, 234)">.</span><span class="token function" style="color:rgb(130, 170, 255)">run_to_completion</span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token number" style="color:rgb(247, 140, 108)">5</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"> </span><span class="token class-name" style="color:rgb(255, 203, 107)">Duration</span><span class="token punctuation" style="color:rgb(199, 146, 234)">::</span><span class="token constant" style="color:rgb(130, 170, 255)">ZERO</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"> </span><span class="token class-name" style="color:rgb(255, 203, 107)">None</span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token operator" style="color:rgb(137, 221, 255)">?</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><br></span></code></pre></div></div>
<p>The pattern is the same in every language: build a template once, clone it per test via the backup API. The FFI detour we took through esqlite is only necessary because our particular Gleam driver doesn't expose it yet.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-full-stack">The Full Stack<a href="https://curling.io/blog/sqlite-test-isolation#the-full-stack" class="hash-link" aria-label="Direct link to The Full Stack" title="Direct link to The Full Stack">​</a></h2>
<p>Here's what our test infrastructure looks like in its entirety:</p>
<ul>
<li><code>test_db.setup()</code> clones a cached template database via SQLite backup API</li>
<li><code>test_db.tz_db()</code> returns a cached timezone database (loaded once, not per test)</li>
<li><code>persistent_term</code> is BEAM's global cache for the template DB and timezone data</li>
<li>12 lines of Erlang FFI wraps the SQLite backup API</li>
</ul>
<p>That's the entire test infrastructure. No test framework plugins, no database cleaner gems, no truncation strategies. SQLite and 12 lines of Erlang.</p>
<hr>
<p><em>This is Part 6 of the Curling IO Foundation series. Next up: <a href="https://curling.io/blog/parallel-tests-for-free">Parallel Tests for Free</a>.</em></p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="foundation" term="foundation"/>
        <category label="sqlite" term="sqlite"/>
        <category label="testing" term="testing"/>
        <category label="architecture" term="architecture"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[What to Look for in Curling Club Management Software]]></title>
        <id>https://curling.io/blog/what-to-look-for-in-curling-club-management-software</id>
        <link href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software"/>
        <updated>2026-03-02T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[A practical guide to evaluating club management software for curling clubs: what matters, what to watch for, and the questions worth asking before you commit.]]></summary>
        <content type="html"><![CDATA[<p>If your curling club is shopping for management software, or wondering whether it's time to replace what you've got, the wrong choice can mean years of workarounds. Here's what to look for.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="purpose-built-for-curling">Purpose-Built for Curling<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#purpose-built-for-curling" class="hash-link" aria-label="Direct link to Purpose-Built for Curling" title="Direct link to Purpose-Built for Curling">​</a></h2>
<p>Generic sports platforms and website builders can handle basic registration, but curling has concepts that most software doesn't account for: ends, hammer, draw schedules, round robins with pools, page playoffs, spare management, rental ice, bonspiels, waitlists, and online waivers.</p>
<p>If the software you're evaluating doesn't understand these things natively, you'll spend your time working around it instead of working with it. Ask whether you can schedule a draw across four sheets, score a game end-by-end, or run a triple knockout bracket without manual intervention.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="management-first-not-a-cms">Management First, Not a CMS<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#management-first-not-a-cms" class="hash-link" aria-label="Direct link to Management First, Not a CMS" title="Direct link to Management First, Not a CMS">​</a></h2>
<p>Some club management software is really just an extension of a content management system (CMS), focused on building websites like it's still 1999. Building a nice website is already a solved problem. Squarespace, Wix, WordPress, and hundreds of others already have that covered. Look for software that has the curling-specific depth you need, not a website builder with registration bolted on. Most clubs are better off with a simple website any volunteer can update than a giant CMS no one remembers how to administer.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="modern-and-fast">Modern and Fast<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#modern-and-fast" class="hash-link" aria-label="Direct link to Modern and Fast" title="Direct link to Modern and Fast">​</a></h2>
<p>Performance matters more than people think. If the platform feels slow when a curler is trying to register and pay, they notice. Pages need to feel snappy, not just functional. If the platform is also used for provincial or national competitions, it's already proven it can handle real traffic, not a few dozen people checking scores at the same time.</p>
<p>More than half of web traffic comes from phones now. Volunteers entering scores, curlers checking draw times, spectators following results: most of that happens on a phone. The platform needs to work well on smaller screens, desktops included but not prioritized.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="pricing-transparency">Pricing Transparency<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#pricing-transparency" class="hash-link" aria-label="Direct link to Pricing Transparency" title="Direct link to Pricing Transparency">​</a></h2>
<p>Pricing models vary widely. Some platforms charge setup fees, monthly fees, per-member fees, or some combination. Others take a percentage of transactions. Before you sign up, make sure you understand:</p>
<ul>
<li>Is there a cost just to get started?</li>
<li>Are there ongoing fees regardless of usage?</li>
<li>What does payment processing actually cost, all-in?</li>
<li>Are there per-member or per-registration charges on top?</li>
</ul>
<p>Clubs are often volunteer-run with tight budgets. A platform that costs hundreds of dollars a month before a single curler registers doesn't make sense for a 4-sheet club with 120 members.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="online-registration-and-payments">Online Registration and Payments<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#online-registration-and-payments" class="hash-link" aria-label="Direct link to Online Registration and Payments" title="Direct link to Online Registration and Payments">​</a></h2>
<p>If your members can't register and pay online, you're creating work for yourself. Curlers should be able to browse events, register, and pay in one flow. No filling out PDFs, emailing forms, or bringing cheques to the club.</p>
<p>The payment processing should be built in, not bolted on through a third-party plugin. And it should handle what curling clubs deal with: membership fees, family registrations, multiple events in one cart, early bird pricing, discounts, and partial refunds.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="league-and-competition-management">League and Competition Management<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#league-and-competition-management" class="hash-link" aria-label="Direct link to League and Competition Management" title="Direct link to League and Competition Management">​</a></h2>
<p>You should be able to:</p>
<ul>
<li>Generate round robin draw schedules across sheets and time slots</li>
<li>Score games live with optional end scores</li>
<li>Automatically calculate standings with head-to-head tiebreakers</li>
<li>Run playoff brackets that advance teams based on results</li>
<li>Share live scoreboards that spectators and curlers can follow from home</li>
</ul>
<p>If the platform can't handle a standard 8-team round robin on 4 sheets without manual scheduling, it's not built for curling.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="data-portability-and-accounting-integration">Data Portability and Accounting Integration<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#data-portability-and-accounting-integration" class="hash-link" aria-label="Direct link to Data Portability and Accounting Integration" title="Direct link to Data Portability and Accounting Integration">​</a></h2>
<p>Your data should be yours. Look for the ability to export registration data, financial records, and member information. If you ever want to switch platforms, you shouldn't be locked in.</p>
<p>For clubs that track finances seriously, look for accrual, double-entry accounting and integration with software like QuickBooks, Xero, or Sage. Ask whether the platform can export transactions in a format your bookkeeper can actually use.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="track-record-development-and-support">Track Record, Development, and Support<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#track-record-development-and-support" class="hash-link" aria-label="Direct link to Track Record, Development, and Support" title="Direct link to Track Record, Development, and Support">​</a></h2>
<p>Who else is using the platform? A tool used by a handful of clubs is a different proposition than one trusted by national organizations and hundreds of clubs. Is the software still being actively improved? How often do updates ship? Some platforms haven't changed in years, and it shows.</p>
<p>Support turnaround matters too. When something breaks during league night or a bonspiel weekend, you need a response in hours, not weeks. Ask other clubs what their experience has been. Community adoption also means your curlers are more likely to already have an account, which makes registration smoother for everyone.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="who-owns-it">Who Owns It<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#who-owns-it" class="hash-link" aria-label="Direct link to Who Owns It" title="Direct link to Who Owns It">​</a></h2>
<p>It's worth looking past the product and asking who actually owns the company behind it. A platform built and run by people in the curling community is a different thing than one owned by an investment firm in another country. When a private equity group acquires a software company, the priorities tend to shift: prices go up, support gets outsourced, and development slows down or focuses on whatever makes the numbers look good for the next quarter. The curling world is small and mostly volunteer-driven. You want the people making decisions about your software to understand that.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="ai-ready">AI Ready<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#ai-ready" class="hash-link" aria-label="Direct link to AI Ready" title="Direct link to AI Ready">​</a></h2>
<p>Are you already using chatbots like Claude.ai to help with day-to-day tasks? A lot of club managers are. As these tools get more capable, they'll be able to do more than just answer questions. Look for a platform that's ready for this. Setting up a draw, checking registration numbers, pulling a financial report: these are things your AI assistant should eventually be able to do for you, if the software supports it. Your members could benefit too, using their own chatbots to register for events or set up reminders for upcoming matches.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="ask-the-right-questions">Ask the Right Questions<a href="https://curling.io/blog/what-to-look-for-in-curling-club-management-software#ask-the-right-questions" class="hash-link" aria-label="Direct link to Ask the Right Questions" title="Direct link to Ask the Right Questions">​</a></h2>
<p>Full disclosure: we built <a href="https://curling.io/" target="_blank" rel="noopener noreferrer">Curling IO</a> to check all of these boxes. But regardless of what you choose, these are the questions worth asking.</p>]]></content>
        <author>
            <name>Chris</name>
        </author>
        <category label="club-management" term="club-management"/>
        <category label="buyers-guide" term="buyers-guide"/>
        <category label="choosing-software" term="choosing-software"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[Why We Chose SQLite]]></title>
        <id>https://curling.io/blog/why-we-chose-sqlite</id>
        <link href="https://curling.io/blog/why-we-chose-sqlite"/>
        <updated>2026-02-27T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[We assumed PostgreSQL for Version 3. After a decade running Postgres in production, why would we even consider something else? We knew the tooling, the failure modes, the operational playbook. Postgres is the safe choice for good reason.]]></summary>
        <content type="html"><![CDATA[<p>We assumed PostgreSQL for Version 3. After a decade running Postgres in production, why would we even consider something else? We knew the tooling, the failure modes, the operational playbook. Postgres is the safe choice for good reason.</p>
<p>Then we looked at what "self-hosting Postgres" actually involves, compared it to what Litestream does for SQLite, and changed our minds. This post covers the decision, the architecture, the trade-offs, and why we'd make the same call again.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="what-version-2-runs-on">What Version 2 Runs On<a href="https://curling.io/blog/why-we-chose-sqlite#what-version-2-runs-on" class="hash-link" aria-label="Direct link to What Version 2 Runs On" title="Direct link to What Version 2 Runs On">​</a></h2>
<p>Curling IO Version 2 runs on Rails backed by PostgreSQL on <a href="https://www.crunchydata.com/products/crunchy-bridge" target="_blank" rel="noopener noreferrer">Crunchy Bridge</a>, a fantastic managed Postgres service. It works. Crunchy Data handles backups, failover, and tuning. We don't think about <code>shared_buffers</code> or <code>autovacuum</code> settings. We don't run pgBackRest or schedule base backups. (Crunchy Data was <a href="https://www.crunchydata.com/blog/crunchy-data-joins-snowflake" target="_blank" rel="noopener noreferrer">acquired by Snowflake</a> in 2025, which adds another reason to reduce our dependency on third-party managed services.)</p>
<p>The trade-off is cost, lock-in, and jurisdiction. Crunchy Bridge is AWS-only, so the app servers have to live on AWS too. That's the full stack dependent on a single US cloud provider. We're a 100% Canadian company and we'd rather keep our infrastructure closer to home, especially given the current political uncertainty south of the border.</p>
<p>For Version 3, we're moving to OVH, a French-owned provider with data centers in Quebec and Ontario. All data stays in Canada. Crunchy Bridge doesn't run on OVH, so we can't bring it along. Self-hosting Postgres on OVH would mean taking on everything Crunchy handles today: connection pooling, server tuning (<code>shared_buffers</code>, <code>effective_cache_size</code>, <code>work_mem</code>, <code>max_connections</code>), autovacuum monitoring, and the full backup story (pgBackRest, WAL archiving, scheduled base backups, tested restore procedures, monitoring backup freshness). That's a lot to take on for a small organization.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-architecture-database-per-sport">The Architecture: Database Per Sport<a href="https://curling.io/blog/why-we-chose-sqlite#the-architecture-database-per-sport" class="hash-link" aria-label="Direct link to The Architecture: Database Per Sport" title="Direct link to The Architecture: Database Per Sport">​</a></h2>
<p>Version 3 uses a database-per-sport architecture. Each sport gets its own SQLite file with an identical schema:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">db/</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">├── curling.db          # All curling club data</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">├── pickleball.db       # All pickleball club data</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">└── shared.db           # Cross-sport reference data, jobs, email suppressions</span><br></span></code></pre></div></div>
<p>Each sport database is multi-tenant. Every table with tenant data includes an <code>org_id</code> column, and every query filters by it. Adding a new sport means creating a new database file with the same schema. No cross-sport queries, no shared-database contention.</p>
<p><code>shared.db</code> holds things that span sports: Canadian tax jurisdictions (17 rows covering GST, HST, PST, and QST rates by province), the <a href="https://curling.io/blog/background-jobs-without-the-baggage">background job queue</a>, and email suppression lists from Postmark webhooks.</p>
<p>At startup, the server opens one connection per database and holds it for the lifetime of the app:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn open(path: String) -&gt; Result(sqlight.Connection, sqlight.Error) {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  use conn &lt;- result.try(sqlight.open(path))</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  use _ &lt;- result.try(sqlight.exec("PRAGMA journal_mode=WAL;", conn))</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  use _ &lt;- result.try(sqlight.exec("PRAGMA busy_timeout=5000;", conn))</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  use _ &lt;- result.try(sqlight.exec("PRAGMA foreign_keys=ON;", conn))</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  Ok(conn)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>Three PRAGMAs on every connection: WAL mode for concurrent reads, a 5-second busy timeout so writes queue instead of failing immediately, and foreign key enforcement (which SQLite disables by default).</p>
<p>One connection per database. No connection pool. No pool configuration. Each incoming HTTP request gets the sport-specific connection (resolved from the hostname) plus the shared connection.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-in-process-sqlite">Why In-Process SQLite<a href="https://curling.io/blog/why-we-chose-sqlite#why-in-process-sqlite" class="hash-link" aria-label="Direct link to Why In-Process SQLite" title="Direct link to Why In-Process SQLite">​</a></h2>
<p>SQLite runs inside the application process. There's no socket, no TCP round-trip, no serialization between the app and the database. A query is a function call.</p>
<p>Our workload is primarily simple indexed lookups: single-row fetches by primary key, filtered lists by <code>org_id</code>, a handful of line items joined to an order. SQLite handles tens of thousands of write transactions per second, far beyond what 1,000+ curling clubs will generate. Even during peak registration or when a provincial championship is posting live scores, the load profile is overwhelmingly reads with occasional write bursts.</p>
<p>The cost follows from the architecture. No separate database server means no database hosting bill. The entire v3 stack runs on a single server for less than what managed Postgres alone costs on AWS.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="wal-mode-and-concurrency">WAL Mode and Concurrency<a href="https://curling.io/blog/why-we-chose-sqlite#wal-mode-and-concurrency" class="hash-link" aria-label="Direct link to WAL Mode and Concurrency" title="Direct link to WAL Mode and Concurrency">​</a></h2>
<p>WAL (Write-Ahead Logging) is what makes SQLite viable for a concurrent web application. Without it, any write locks the entire database for both readers and writers. With WAL, readers proceed concurrently with writes. On the BEAM, where hundreds of lightweight processes might query simultaneously during a traffic spike, this is essential.</p>
<p>Writes are still serialized. One writer at a time, coordinated by a mutex in the SQLite library. If the write lock can't be acquired within the 5-second busy timeout, SQLite returns <code>SQLITE_BUSY</code>. With our read-heavy workload, write contention hasn't been a practical concern. This is why we use a <a href="https://curling.io/blog/background-jobs-without-the-baggage">separate database for background jobs</a>. Job processing writes to <code>shared.db</code>, not the sport databases, so it never contends with registration queries. With SQLite's single-writer model, separating write-heavy workloads into different files is a real architectural consideration, not just organization.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="litestream-the-tipping-point">Litestream: The Tipping Point<a href="https://curling.io/blog/why-we-chose-sqlite#litestream-the-tipping-point" class="hash-link" aria-label="Direct link to Litestream: The Tipping Point" title="Direct link to Litestream: The Tipping Point">​</a></h2>
<p>This is what tipped the scales.</p>
<p>SQLite is a file. <a href="https://litestream.io/" target="_blank" rel="noopener noreferrer">Litestream</a> watches that file and continuously replicates its WAL frames to S3-compatible object storage. For us, that's OVH Object Storage in two different geographic locations in Canada. Changes sync every 10 seconds to both.</p>
<p>The configuration is one YAML file:</p>
<div class="language-yaml codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-yaml codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token key atrule">dbs</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  </span><span class="token punctuation" style="color:rgb(199, 146, 234)">-</span><span class="token plain"> </span><span class="token key atrule">path</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> /var/lib/curling</span><span class="token punctuation" style="color:rgb(199, 146, 234)">-</span><span class="token plain">io/databases/curling.db</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    </span><span class="token key atrule">replicas</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      </span><span class="token punctuation" style="color:rgb(199, 146, 234)">-</span><span class="token plain"> </span><span class="token key atrule">type</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> s3</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        </span><span class="token key atrule">bucket</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> xxxxx</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        </span><span class="token key atrule">endpoint</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> xxxxx</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        </span><span class="token key atrule">sync-interval</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> 10s</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      </span><span class="token punctuation" style="color:rgb(199, 146, 234)">-</span><span class="token plain"> </span><span class="token key atrule">type</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> s3</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        </span><span class="token key atrule">bucket</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> xxxxx</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        </span><span class="token key atrule">endpoint</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> xxxxx</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        </span><span class="token key atrule">sync-interval</span><span class="token punctuation" style="color:rgb(199, 146, 234)">:</span><span class="token plain"> 10s</span><br></span></code></pre></div></div>
<p>Recovery is one command:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">litestream restore -o /path/to/curling.db s3://xxxxx/curling.db</span><br></span></code></pre></div></div>
<p>That produces a ready-to-use database file. Copy it to the right path, start the app.</p>
<p>Now compare that to self-hosting PostgreSQL backup and recovery:</p>
<ol>
<li>Install pgBackRest</li>
<li>Configure a stanza, set <code>archive_mode</code> and <code>archive_command</code> in <code>postgresql.conf</code></li>
<li>Schedule base backups via cron</li>
<li>Monitor backup success, WAL retention, and disk usage</li>
<li>Periodically test restores (because an untested backup isn't a backup)</li>
<li>When disaster strikes: stop PostgreSQL, run <code>pgbackrest restore --type=time --target="..."</code>, wait for WAL replay, verify the recovery point, promote to primary</li>
</ol>
<p>PostgreSQL gives you something Litestream doesn't: exact transaction-level point-in-time recovery. You can roll back to any specific transaction. Litestream gives you "latest snapshot plus WAL frames," which means a worst-case recovery gap of about 10 seconds. For our use case that's more than sufficient.</p>
<p>The operational gap is where it gets decisive. Litestream is one binary, one config file, near-zero ongoing maintenance. PostgreSQL backup is a practice, something you rehearse, monitor, and maintain over the life of the project. Even with perfect tooling, you can't reduce "restore a running server's state" to "download a file and start the app." That gap is architectural. It won't close with better Postgres tooling because the complexity is inherent to backing up a running server process.</p>
<p>The simplicity ceiling for SQLite backup is fundamentally higher than what's possible for any client-server database.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-sqlite-tax">The SQLite Tax<a href="https://curling.io/blog/why-we-chose-sqlite#the-sqlite-tax" class="hash-link" aria-label="Direct link to The SQLite Tax" title="Direct link to The SQLite Tax">​</a></h2>
<p>Here's what we pay for that simplicity. Every item below is something PostgreSQL handles natively.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="no-real-types">No Real Types<a href="https://curling.io/blog/why-we-chose-sqlite#no-real-types" class="hash-link" aria-label="Direct link to No Real Types" title="Direct link to No Real Types">​</a></h3>
<p>SQLite doesn't have booleans, dates, or enums. Booleans are stored as INTEGER 0/1. Dates are Unix epoch integers. Enums are TEXT strings.</p>
<p>Every boolean parameter needs a <code>db.bool_to_int(value)</code> call. Every boolean decoder needs a <code>!= 0</code> check. All date formatting, timezone conversion, and comparison lives in application code. There's no <code>DATE_TRUNC</code>, no <code>INTERVAL</code>, no <code>AT TIME ZONE</code>.</p>
<p>We use <a href="https://www.sqlite.org/stricttables.html" target="_blank" rel="noopener noreferrer">STRICT tables</a> on everything, which enforces that values match declared column types (INTEGER, TEXT, REAL, BLOB). It's a free safety net for anything that bypasses the application layer, but it doesn't help with booleans, dates, or enums. Those stay as application-level concerns, but since Gleam and <a href="https://hexdocs.pm/parrot/" target="_blank" rel="noopener noreferrer">Parrot</a> are the only source of writes and types are enforced there, it's manageable.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="limited-alter-table">Limited ALTER TABLE<a href="https://curling.io/blog/why-we-chose-sqlite#limited-alter-table" class="hash-link" aria-label="Direct link to Limited ALTER TABLE" title="Direct link to Limited ALTER TABLE">​</a></h3>
<p>SQLite stores its schema as the original <code>CREATE TABLE</code> text, not as structured system catalogs like PostgreSQL. This means <code>ALTER TABLE</code> is minimal:</p>
<ul>
<li><code>ADD COLUMN</code>, <code>RENAME COLUMN</code>, <code>DROP COLUMN</code> all work</li>
<li>Changing a column's type, default, or <code>NOT NULL</code> does not</li>
<li>Adding or modifying <code>CHECK</code> or <code>FOREIGN KEY</code> constraints does not</li>
<li>Adding <code>UNIQUE</code> to an existing column does not</li>
</ul>
<p>Changing a column post-launch requires a four-step dance: add the new column, copy the data, drop the old column, rename. PostgreSQL handles this with a single <code>ALTER COLUMN</code>.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="no-check-constraints">No CHECK Constraints<a href="https://curling.io/blog/why-we-chose-sqlite#no-check-constraints" class="hash-link" aria-label="Direct link to No CHECK Constraints" title="Direct link to No CHECK Constraints">​</a></h3>
<p>We don't use CHECK constraints anywhere. This is a policy decision, and not one we're thrilled about.</p>
<p>CHECK constraints are embedded in the <code>CREATE TABLE</code> DDL string. Modifying one means a full table rebuild. Adding a new enum value shouldn't require that. So all validation lives in Gleam: union types for enums (with exhaustive pattern matching at compile time), validation functions for business logic, and <code>let assert</code> in decoders to crash fast if bad data gets in.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="write-serialization">Write Serialization<a href="https://curling.io/blog/why-we-chose-sqlite#write-serialization" class="hash-link" aria-label="Direct link to Write Serialization" title="Direct link to Write Serialization">​</a></h3>
<p>As covered above, writes are serialized. In practice this barely matters because there's no network round-trip and no protocol overhead. A write is a function call that completes in microseconds. The lock is held for such a short window that writers rarely wait for each other at all. But it does rule out horizontal scaling to multiple app servers writing to the same database.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="single-node-only">Single Node Only<a href="https://curling.io/blog/why-we-chose-sqlite#single-node-only" class="hash-link" aria-label="Direct link to Single Node Only" title="Direct link to Single Node Only">​</a></h3>
<p>SQLite is a file on local disk. Multiple app servers can't share it (NFS breaks SQLite's file locking and leads to corruption). We're locked into a single-server architecture. On a BEAM runtime that handles massive concurrency on one node, this is fine for well over 100,000 clubs. It's a hard ceiling if we ever need more, and if we do, that's a really good problem to have.</p>
<p>There are projects that add distributed replication to SQLite (LiteFS, rqlite, dqlite), but they add significant complexity: FUSE filesystems, consensus protocols, additional infrastructure. At that point you're fighting to make SQLite behave like Postgres, and you should probably just use Postgres.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="nif-risk">NIF Risk<a href="https://curling.io/blog/why-we-chose-sqlite#nif-risk" class="hash-link" aria-label="Direct link to NIF Risk" title="Direct link to NIF Risk">​</a></h3>
<p>Our SQLite driver (<a href="https://github.com/mmzeeman/esqlite" target="_blank" rel="noopener noreferrer">esqlite</a>) is an Erlang NIF - compiled C code that runs outside the BEAM scheduler's control. A long-running query blocks a scheduler thread. A segfault takes down the entire VM.</p>
<p>In practice, SQLite's C library is near-bulletproof, and our queries return in microseconds. We mitigate the risk by keeping all queries simple (indexed lookups, no full table scans) and pushing anything heavy to background jobs. But the risk surface exists.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="sql-dialect-gaps">SQL Dialect Gaps<a href="https://curling.io/blog/why-we-chose-sqlite#sql-dialect-gaps" class="hash-link" aria-label="Direct link to SQL Dialect Gaps" title="Direct link to SQL Dialect Gaps">​</a></h3>
<p>Small things that add friction:</p>
<ul>
<li><code>DELETE FROM t WHERE ... LIMIT 1</code> doesn't work. You need a subquery: <code>DELETE FROM t WHERE id = (SELECT id FROM t WHERE ... LIMIT 1)</code>.</li>
<li>No <code>LISTEN/NOTIFY</code> for real-time push from the database. Change notification goes through application-level pub/sub.</li>
<li><code>NULL</code> comparisons need <code>IS ?</code> instead of <code>= ?</code> when the parameter can be NULL.</li>
</ul>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="why-we-accept-it">Why We Accept It<a href="https://curling.io/blog/why-we-chose-sqlite#why-we-accept-it" class="hash-link" aria-label="Direct link to Why We Accept It" title="Direct link to Why We Accept It">​</a></h3>
<p>Every tax above is an application-level workaround. Type conversions, date formatting, schema evolution dances, validation in Gleam instead of CHECK constraints. None of them change the architecture. They're annoyances, not obstacles.</p>
<p>The things that would actually force a migration (write contention, multi-node scaling, database size) aren't happening at 1,000 clubs with a read-heavy workload on the BEAM.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="when-wed-switch-to-postgresql">When We'd Switch to PostgreSQL<a href="https://curling.io/blog/why-we-chose-sqlite#when-wed-switch-to-postgresql" class="hash-link" aria-label="Direct link to When We'd Switch to PostgreSQL" title="Direct link to When We'd Switch to PostgreSQL">​</a></h2>
<p>Honestly, we don't see it happening anytime soon. Most of the limits people worry about with SQLite (database size, write throughput, busy errors) can be pushed further by upgrading the server. Vertical scaling is cheap and simple, and the BEAM makes excellent use of whatever hardware you give it.</p>
<p>The only real trigger would be needing multiple application servers, and that would mean we've seriously outgrown our predictions or miscalculated. If that happens, the migration path is mechanical, not architectural. Swap <code>sqlight</code> for <code>gleam_pgo</code>. Adjust SQL dialect across all query files: <code>?</code> becomes <code>$1, $2, ...</code>, <code>INTEGER PRIMARY KEY</code> becomes <code>GENERATED ALWAYS AS IDENTITY</code>, booleans become real booleans, remove the <code>DELETE</code> subquery workarounds. Separate <code>.db</code> files map to PostgreSQL schemas within a single database. Business logic and request handlers stay the same.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="whats-next">What's Next<a href="https://curling.io/blog/why-we-chose-sqlite#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next">​</a></h2>
<p>If SQLite hits a wall, we switch to Postgres. The exit strategy is defined and the migration is a dialect swap, not a rewrite.</p>
<p>But so far, Litestream plus in-process SQLite has been one of the best decisions in the v3 rewrite. And there's one more benefit worth its own post: because SQLite runs in-process, every test gets its own in-memory database. No shared state, no cleanup, no flaky tests. That's next.</p>
<hr>
<p><em>This is Part 5 of the Curling IO Foundation series. Next up: <a href="https://curling.io/blog/sqlite-test-isolation">Test Isolation for Free with SQLite</a>.</em></p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="foundation" term="foundation"/>
        <category label="sqlite" term="sqlite"/>
        <category label="architecture" term="architecture"/>
        <category label="gleam" term="gleam"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[How Curling Canada Uses Curling IO for National Competitions]]></title>
        <id>https://curling.io/blog/how-curling-canada-uses-curling-io</id>
        <link href="https://curling.io/blog/how-curling-canada-uses-curling-io"/>
        <updated>2026-02-26T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Curling Canada runs all of their national championships through Curling IO, from the Brier to the Scotties to the Trials. The same platform is available to any curling club.]]></summary>
        <content type="html"><![CDATA[<p>If you've ever checked a scoreboard during the Brier or the Scotties Tournament of Hearts, you've seen Curling IO in action. Since 2014, Curling Canada has used Curling IO to manage their national competitions, and the same platform is available to any curling club.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="powering-canadas-biggest-curling-events">Powering Canada's Biggest Curling Events<a href="https://curling.io/blog/how-curling-canada-uses-curling-io#powering-canadas-biggest-curling-events" class="hash-link" aria-label="Direct link to Powering Canada's Biggest Curling Events" title="Direct link to Powering Canada's Biggest Curling Events">​</a></h2>
<p>That includes the events that curling fans across the country follow every year:</p>
<ul>
<li>The Brier (Montana's Brier, formerly Tim Hortons Brier)</li>
<li>Scotties Tournament of Hearts</li>
<li>Canadian Mixed Doubles Championship</li>
<li>Canadian Curling Trials and Pre-Trials</li>
<li>Canadian Under-20 and Under-18 Championships</li>
<li>Canadian Seniors Championships</li>
<li>Canadian Wheelchair Curling Championship</li>
<li>Canadian Mixed Curling Championship</li>
<li>Canadian Club Curling Championships</li>
</ul>
<p>In total, Curling Canada has managed over 360 competitions through Curling IO, with more than 20,000 games scored on the platform.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="what-curling-io-handles">What Curling IO Handles<a href="https://curling.io/blog/how-curling-canada-uses-curling-io#what-curling-io-handles" class="hash-link" aria-label="Direct link to What Curling IO Handles" title="Direct link to What Curling IO Handles">​</a></h2>
<p>For each competition, Curling IO handles registration and team assembly, draw scheduling with round robins and playoff brackets across sheets and time slots. Games are scored live with end scores and shot-by-shot tracking, and standings and bracket advancement update automatically.</p>
<p>When fans check scores on <a href="https://www.curling.ca/scoreboard/" target="_blank" rel="noopener noreferrer">curling.ca/scoreboard</a> during a national event, both the data and the scoreboard widgets they're looking at were built by us.</p>
<p>During major events like the Brier, with competitions running simultaneously across the country, traffic spikes well beyond what any individual club would ever generate, and Curling IO handles it smoothly.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-same-platform-your-club-can-use">The Same Platform Your Club Can Use<a href="https://curling.io/blog/how-curling-canada-uses-curling-io#the-same-platform-your-club-can-use" class="hash-link" aria-label="Direct link to The Same Platform Your Club Can Use" title="Direct link to The Same Platform Your Club Can Use">​</a></h2>
<p>The draw scheduling, scoring, and bracket tools that Curling Canada relies on for national championships are the same tools you'd use for your Tuesday night league or weekend bonspiel. We handle a 4-sheet club's leagues the same way we handle a nationally televised championship.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="built-for-curling-not-adapted-for-it">Built for Curling, Not Adapted for It<a href="https://curling.io/blog/how-curling-canada-uses-curling-io#built-for-curling-not-adapted-for-it" class="hash-link" aria-label="Direct link to Built for Curling, Not Adapted for It" title="Direct link to Built for Curling, Not Adapted for It">​</a></h2>
<p>Curling has concepts that generic sports software doesn't account for: ends, hammer, last stone advantage, round robins with multiple pools, page playoffs, mixed doubles scoring. Even draw scheduling has curling-specific nuances, like minimizing how often a team plays on the same sheet. We even published a free draw schedule tool at <a href="https://curlingschedules.com/" target="_blank" rel="noopener noreferrer">curlingschedules.com</a> that anyone can use.</p>
<p>Curling IO was built around these concepts. It's why Curling Canada chose it, and it's why all 14 provincial and territorial curling associations in Canada use it too.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="see-it-in-action">See It in Action<a href="https://curling.io/blog/how-curling-canada-uses-curling-io#see-it-in-action" class="hash-link" aria-label="Direct link to See It in Action" title="Direct link to See It in Action">​</a></h2>
<p>You can browse Curling Canada's competitions and results at <a href="https://canada.curling.io/" target="_blank" rel="noopener noreferrer">canada.curling.io</a>. Every draw schedule, scoreboard, and bracket you see there runs on Curling IO.</p>
<p>If your club wants the same tools that power Canadian curling at the national level, <a href="https://curling.io/docs/getting-started/curling-club-managers">get started here</a>. There are no setup fees or monthly fees. Check out our <a href="https://curling.io/docs/getting-started/pricing">pricing page</a> for a full feature breakdown and cost calculator.</p>]]></content>
        <author>
            <name>Chris</name>
        </author>
        <category label="curling-canada" term="curling-canada"/>
        <category label="national-competitions" term="national-competitions"/>
        <category label="live-scoring" term="live-scoring"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[Background Jobs Without the Baggage]]></title>
        <id>https://curling.io/blog/background-jobs-without-the-baggage</id>
        <link href="https://curling.io/blog/background-jobs-without-the-baggage"/>
        <updated>2026-02-25T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[In most web stacks, adding background jobs means adding infrastructure: Redis, Sidekiq, a separate worker process, a monitoring dashboard, another thing to deploy and keep running. Curling IO Version 2 uses Delayed Job backed by PostgreSQL, which works well but requires a separate worker daemon alongside the web process.]]></summary>
        <content type="html"><![CDATA[<p>In most web stacks, adding background jobs means adding infrastructure: Redis, Sidekiq, a separate worker process, a monitoring dashboard, another thing to deploy and keep running. Curling IO Version 2 uses Delayed Job backed by PostgreSQL, which works well but requires a separate worker daemon alongside the web process.</p>
<p>Curling IO Version 3 runs on the BEAM (Erlang's virtual machine), and background jobs are just another process in the same runtime. No Redis. No separate worker. No additional infrastructure. This post covers how we built it, why we chose SQLite persistence over in-memory queues, and how the whole thing fits into a few hundred lines of Gleam.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-beam-advantage">The BEAM Advantage<a href="https://curling.io/blog/background-jobs-without-the-baggage#the-beam-advantage" class="hash-link" aria-label="Direct link to The BEAM Advantage" title="Direct link to The BEAM Advantage">​</a></h2>
<p>The BEAM VM was designed for telecom systems that needed to handle millions of concurrent operations without downtime. Every BEAM application already has lightweight processes, supervisors, and message passing built in. These aren't OS threads. They're managed by the VM's own scheduler, and you can run hundreds of thousands of them in a single OS process.</p>
<p>This means a "background worker" isn't a separate service. It's just another process running alongside your HTTP handlers, sharing the same runtime and the same log output. Starting one is a function call, not a deployment.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-not-just-fire-and-forget">Why Not Just Fire-and-Forget?<a href="https://curling.io/blog/background-jobs-without-the-baggage#why-not-just-fire-and-forget" class="hash-link" aria-label="Direct link to Why Not Just Fire-and-Forget?" title="Direct link to Why Not Just Fire-and-Forget?">​</a></h2>
<p>The simplest approach on BEAM would be to spawn a process for each job and let it run. Or use an OTP actor with in-memory messages. We considered this, but it has a gap: if the server restarts, every pending job disappears. A lost login email is minor (the user just requests another), but we'll also be running draw schedule generation, payment processing through Stripe, and accounting syncs. Losing those mid-flight is a real problem.</p>
<p>We wanted durability without complexity. SQLite was already there.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="a-separate-database">A Separate Database<a href="https://curling.io/blog/background-jobs-without-the-baggage#a-separate-database" class="hash-link" aria-label="Direct link to A Separate Database" title="Direct link to A Separate Database">​</a></h2>
<p>Curling IO already uses SQLite for its sport-specific databases (one per sport). We could have added a <code>jobs</code> table there, but we deliberately put it in a separate <code>shared.db</code>. Background job processing means frequent writes (insert job, mark running, mark completed), and there's no reason to contend for write locks on the sport databases. SQLite uses a write-ahead log with a single writer, so separating the workloads means job processing never blocks a registration query and vice versa.</p>
<p>The jobs table:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token keyword" style="font-style:italic">CREATE</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">TABLE</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">IF</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">EXISTS</span><span class="token plain"> jobs </span><span class="token punctuation" style="color:rgb(199, 146, 234)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  id </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">PRIMARY</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">KEY</span><span class="token plain"> AUTOINCREMENT</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  kind </span><span class="token keyword" style="font-style:italic">TEXT</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  payload </span><span class="token keyword" style="font-style:italic">TEXT</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  </span><span class="token keyword" style="font-style:italic">status</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">TEXT</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">DEFAULT</span><span class="token plain"> </span><span class="token string" style="color:rgb(195, 232, 141)">'pending'</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  attempts </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">DEFAULT</span><span class="token plain"> </span><span class="token number" style="color:rgb(247, 140, 108)">0</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  max_attempts </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">DEFAULT</span><span class="token plain"> </span><span class="token number" style="color:rgb(247, 140, 108)">3</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  max_running_seconds </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">DEFAULT</span><span class="token plain"> </span><span class="token number" style="color:rgb(247, 140, 108)">120</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  error </span><span class="token keyword" style="font-style:italic">TEXT</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  created_at </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  run_at </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">NOT</span><span class="token plain"> </span><span class="token boolean" style="color:rgb(255, 88, 116)">NULL</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  started_at </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  completed_at </span><span class="token keyword" style="font-style:italic">INTEGER</span><span class="token plain"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain"></span><span class="token punctuation" style="color:rgb(199, 146, 234)">)</span><span class="token plain"> STRICT</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><br></span></code></pre></div></div>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="enqueue-is-just-an-insert">Enqueue Is Just an INSERT<a href="https://curling.io/blog/background-jobs-without-the-baggage#enqueue-is-just-an-insert" class="hash-link" aria-label="Direct link to Enqueue Is Just an INSERT" title="Direct link to Enqueue Is Just an INSERT">​</a></h2>
<p>From a request handler, sending a background email looks like this:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">jobs.enqueue_email(request_context.shared_db, request_context.postmark_api_token, email)</span><br></span></code></pre></div></div>
<p>Under the hood, that's a single <code>INSERT INTO jobs</code> with the email details serialized as JSON in the <code>payload</code> column. It returns in microseconds. The user gets their response immediately instead of waiting 100-500ms for the Postmark API call to complete.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-worker-an-otp-actor">The Worker: An OTP Actor<a href="https://curling.io/blog/background-jobs-without-the-baggage#the-worker-an-otp-actor" class="hash-link" aria-label="Direct link to The Worker: An OTP Actor" title="Direct link to The Worker: An OTP Actor">​</a></h2>
<p>The worker is a single OTP actor, which is BEAM's abstraction for a stateful process that handles messages sequentially. Ours has exactly one message type:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub type Message {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  CheckForWork</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>On startup, the actor sends itself a <code>CheckForWork</code> message after a one-second delay. When it handles that message, it queries for pending jobs, processes them, and schedules the next check. It's a self-sustaining polling loop:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">fn handle_message(state, msg) {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  case msg {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    CheckForWork -&gt; {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      process_pending_jobs(state)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      process.send_after(state.self, 1000, CheckForWork)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      actor.continue(state)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>Each poll picks up a batch of pending jobs where <code>run_at</code> is in the past, processes them one at a time, and marks each as completed or failed. The actor never crashes on a bad job. Errors are caught and logged, and the actor keeps processing the rest of the batch.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="retry-with-exponential-backoff">Retry With Exponential Backoff<a href="https://curling.io/blog/background-jobs-without-the-baggage#retry-with-exponential-backoff" class="hash-link" aria-label="Direct link to Retry With Exponential Backoff" title="Direct link to Retry With Exponential Backoff">​</a></h2>
<p>Not every failure is permanent. Postmark might be briefly unreachable, or return a 500. The retry logic is straightforward: if a job fails and has attempts remaining, set it back to <code>pending</code> with a future <code>run_at</code>. The backoff is exponential (5 seconds, 25 seconds, 125 seconds):</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">Attempt 1 fails → retry in 5s</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">Attempt 2 fails → retry in 25s</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">Attempt 3 fails → permanently failed</span><br></span></code></pre></div></div>
<p>Three attempts is the default, configurable per job via the <code>max_attempts</code> column. A permanently failed job stays in the table with its error message, so you can see exactly what went wrong:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token keyword" style="font-style:italic">SELECT</span><span class="token plain"> id</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"> kind</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"> error</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"> attempts </span><span class="token keyword" style="font-style:italic">FROM</span><span class="token plain"> jobs </span><span class="token keyword" style="font-style:italic">WHERE</span><span class="token plain"> </span><span class="token keyword" style="font-style:italic">status</span><span class="token plain"> </span><span class="token operator" style="color:rgb(137, 221, 255)">=</span><span class="token plain"> </span><span class="token string" style="color:rgb(195, 232, 141)">'failed'</span><span class="token punctuation" style="color:rgb(199, 146, 234)">;</span><br></span></code></pre></div></div>
<p>No separate monitoring dashboard needed. It's just SQL.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="supervised-for-resilience">Supervised for Resilience<a href="https://curling.io/blog/background-jobs-without-the-baggage#supervised-for-resilience" class="hash-link" aria-label="Direct link to Supervised for Resilience" title="Direct link to Supervised for Resilience">​</a></h2>
<p>The actor runs under an OTP supervisor. If the actor process dies, the supervisor restarts it automatically under the same registered name. The polling loop resumes, and any jobs that were marked <code>running</code> when the crash happened get recovered by a cleanup sweep.</p>
<p>Each job has a <code>max_running_seconds</code> column (default: 120 seconds). At the start of every poll cycle, the actor checks for jobs that have been in <code>running</code> status longer than their timeout and resets them back to <code>pending</code>. Since the <code>attempts</code> counter was already incremented when the job entered <code>running</code>, the existing retry and backoff logic handles the rest, including capping retries at <code>max_attempts</code>. Completed and failed jobs are purged after 7 days, which is plenty of time to debug anything that goes wrong.</p>
<p>This is the BEAM's bread and butter. Erlang's "let it crash" philosophy means you don't write defensive code to prevent every possible failure. You write a supervisor that recovers from it, and a cleanup sweep that catches anything that slips through. The result is less code that's more resilient.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="the-full-architecture">The Full Architecture<a href="https://curling.io/blog/background-jobs-without-the-baggage#the-full-architecture" class="hash-link" aria-label="Direct link to The Full Architecture" title="Direct link to The Full Architecture">​</a></h2>
<p>Here's what happens when a user requests a login email:</p>
<ol>
<li><strong>Request handler</strong> validates the email, generates a token, and calls <code>jobs.enqueue_email()</code>, which is just an INSERT into <code>shared.db</code> (microseconds)</li>
<li><strong>Response</strong> returns immediately with the "check your email" page</li>
<li><strong>Jobs actor</strong> picks up the pending job on its next poll (within 1 second)</li>
<li><strong>Actor</strong> marks the job as <code>running</code>, calls the Postmark API, marks it <code>completed</code></li>
<li>If Postmark is down, the job goes back to <code>pending</code> with a backoff delay</li>
</ol>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="what-we-didnt-need">What We Didn't Need<a href="https://curling.io/blog/background-jobs-without-the-baggage#what-we-didnt-need" class="hash-link" aria-label="Direct link to What We Didn't Need" title="Direct link to What We Didn't Need">​</a></h2>
<p>Here's what's absent from this setup:</p>
<ul>
<li>No Redis. The queue is a SQLite table.</li>
<li>No separate worker process. The actor runs in the same BEAM VM.</li>
<li>No job framework. It's a few hundred lines of Gleam.</li>
<li>No monitoring service. Failed jobs are just rows in a table.</li>
<li>No deployment coordination. One binary, one process.</li>
</ul>
<p>This setup can comfortably scale to tens of thousands of clubs. SQLite handles the throughput, the BEAM handles the concurrency, and you can read the whole thing in one sitting.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="whats-next">What's Next<a href="https://curling.io/blog/background-jobs-without-the-baggage#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next">​</a></h2>
<p>The jobs system is designed to grow. Adding a new job type means adding a new <code>kind</code> string and a handler function. Long-running report generation, data imports, email campaigns: they all follow the same pattern. INSERT a row, let the actor pick it up.</p>
<hr>
<p><em>This is Part 4 of the Curling IO Foundation series. Next up: <a href="https://curling.io/blog/why-we-chose-sqlite">Why We Chose SQLite</a>.</em></p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="foundation" term="foundation"/>
        <category label="gleam" term="gleam"/>
        <category label="beam" term="beam"/>
        <category label="otp" term="otp"/>
        <category label="architecture" term="architecture"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[Passwordless Auth, Done Right]]></title>
        <id>https://curling.io/blog/passwordless-auth-done-right</id>
        <link href="https://curling.io/blog/passwordless-auth-done-right"/>
        <updated>2026-02-20T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Curling IO has been passwordless since Version 2. No passwords to remember, no passwords to steal, no password reset flows. You enter your email, we send you a short-lived login code, and you're in. It's been working well for over a decade, and for Version 3 we're keeping the same approach while fixing some rough edges and adding multi-email support.]]></summary>
        <content type="html"><![CDATA[<p>Curling IO has been passwordless since Version 2. No passwords to remember, no passwords to steal, no password reset flows. You enter your email, we send you a short-lived login code, and you're in. It's been working well for over a decade, and for Version 3 we're keeping the same approach while fixing some rough edges and adding multi-email support.</p>
<p>But first, let's talk about why we made this <u><em>controversial decision</em></u> in the first place.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-not-passwords">Why Not Passwords?<a href="https://curling.io/blog/passwordless-auth-done-right#why-not-passwords" class="hash-link" aria-label="Direct link to Why Not Passwords?" title="Direct link to Why Not Passwords?">​</a></h2>
<p>The conventional wisdom is that passwords are the "real" way to authenticate and login links are a shortcut. We think it's the opposite. Passwords are the shortcut, and they create more problems than they solve. We're not alone. Slack, Notion, Medium, and Substack all use passwordless login links as their primary authentication method.</p>
<p><strong>Passwords get reused.</strong> Study after study shows that most people reuse the same password across multiple sites. When any one of those sites gets breached, attackers try those credentials everywhere else. This isn't a theoretical risk. It happens constantly, and it's the number one way accounts get compromised. As an application developer, you can't control what your users do on other sites, but you inherit the risk.</p>
<p><strong>Passwords get phished.</strong> A convincing fake login page can harvest credentials at scale. Login links are inherently resistant to phishing because there's no credential to hand over. The code is short-lived and tied to a specific email address.</p>
<p><strong>Passwords require just as much email verification.</strong> Here's the thing people overlook: even with passwords, you still need to verify the user's email. Otherwise anyone can create an account with someone else's email address. So you end up building the same email verification flow that login links use, plus the password management on top of it. You're not avoiding email. You're adding a password layer on top of it.</p>
<p><strong>The industry agrees.</strong> Look at what banks and other high-security sites do now: even after you enter your email and password, they send you a one-time code via text or email. Password alone isn't enough anymore, so they've added the exact same verification step we use. Some have a "remember this device for 2 weeks" checkbox, but in practice it rarely works. You log in an hour later after your session times out and they're sending you another code. It's not just frustrating. It reveals that the password itself has become a meaningless step. The one-time code is doing the actual authentication. The password is just theater you have to sit through before you get to the part that matters. We skip it entirely.</p>
<p><strong>Passwords add surface area.</strong> With passwords, you need: secure hashing (bcrypt/argon2), a "forgot password" flow (which is itself a login link), password strength validation, brute force protection on the login form, and secure storage. Login links need rate limiting and token management too, but the overall surface area is smaller. There's no credential to store long-term, no password policy to enforce, and no "forgot password" flow to build since the login flow <em>is</em> the recovery flow.</p>
<p><strong>"But what about password managers?"</strong> Password managers are great. They solve the reuse and phishing problems for users who use them. But most users don't. And for the curling club use case, where members log in a handful of times per season, asking them to set up and maintain a password manager is unrealistic. Login links meet users where they are.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="how-login-links-are-secure">How Login Links Are Secure<a href="https://curling.io/blog/passwordless-auth-done-right#how-login-links-are-secure" class="hash-link" aria-label="Direct link to How Login Links Are Secure" title="Direct link to How Login Links Are Secure">​</a></h2>
<p>A login link is a short-lived code sent to your email. The security model rests on a simple assumption: if you can read email sent to that address, you control that address. This is the same assumption that password reset flows rely on, but we cut out the middleman.</p>
<p>Here's what makes the implementation secure:</p>
<p><strong>High-entropy tokens.</strong> Each login code is drawn from a high-entropy space, but it doesn't need to be astronomical because of the next two points. The token generator uses Gleam's binary pattern matching to map cryptographically random bytes to an unambiguous alphabet:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn generate_token() -&gt; String {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let alphabet_size = string.length(token_alphabet)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  crypto.strong_random_bytes(token_length)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  |&gt; pick_chars(alphabet_size, "")</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">fn pick_chars(bytes: BitArray, alphabet_size: Int, acc: String) -&gt; String {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  case bytes {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    &lt;&lt;b, rest:bits&gt;&gt; -&gt; {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      let idx = b % alphabet_size</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      let ch = string.slice(token_alphabet, idx, 1)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      pick_chars(rest, alphabet_size, acc &lt;&gt; ch)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    _ -&gt; acc</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p><strong>Aggressive rate limiting.</strong> Authentication attempts are tightly rate limited across multiple layered time windows. Brute-forcing the token space is completely impractical. Each window is checked in sequence using Gleam's <code>use</code> syntax. If any window is exceeded, it short-circuits and returns the retry-after time without checking the rest:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn check_verification_attempt(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  limiter: RateLimiter,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  email: String,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  now: Int,</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">) -&gt; Result(Nil, Int) {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let key = key_prefix &lt;&gt; string.lowercase(email)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  use _ &lt;- result.try(check(limiter, key, attempts_per_minute, 60, now))</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  use _ &lt;- result.try(check(limiter, key, attempts_per_15_minutes, 900, now))</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  use _ &lt;- result.try(check(limiter, key, attempts_per_hour, 3600, now))</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  check(limiter, key, attempts_per_day, 86_400, now)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p><strong>Short-lived.</strong> Each code expires quickly. After verification, the token hash is cleared from the database. There's no window for replay attacks.</p>
<p><strong>Constant-time comparison.</strong> We look up the email row first, then compare the submitted token hash against the stored hash using Gleam's <code>crypto.secure_compare</code>. This takes the same amount of time regardless of where the first mismatch occurs, preventing timing attacks that could leak information about partial matches. If the current token doesn't match, we check the previous token, not to log them in, but to return a specific error message guiding the user to check for a more recent email:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">let hashes_match =</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  crypto.secure_compare(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    bit_array.from_string(submitted_hash),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    bit_array.from_string(stored_hash),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  )</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">case hashes_match {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  True -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    case row.token_expires_at {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      Some(expires_at) if expires_at &gt; now -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        complete_verification(conn, row, now)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      _ -&gt; Error(ExpiredToken)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  False -&gt;</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    case row.previous_token_hash {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      Some(prev_hash) -&gt; {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        let prev_match =</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">          crypto.secure_compare(</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">            bit_array.from_string(submitted_hash),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">            bit_array.from_string(prev_hash),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">          )</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        case prev_match {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">          True -&gt; Error(SupersededToken)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">          False -&gt; Error(InvalidToken)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">        }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">      None -&gt; Error(InvalidToken)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">    }</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>Every branch returns a specific error variant, and the compiler ensures we handle all of them. No forgotten edge cases. The previous token never grants access. It only exists to give a better error message than a generic "invalid token."</p>
<p><strong>No credential storage.</strong> There are no passwords in our database. In a breach scenario, attackers get token hashes that are short-lived. Compare that to a password database where every hash is a target for offline cracking.</p>
<p>It's also worth noting that the main vulnerability of login links is a compromised email account. But if someone's email is compromised, they have much bigger problems than their Curling IO profile. And a password-based system is equally vulnerable in that scenario. The attacker just clicks "forgot password" and they're in.</p>
<p>The bottom line: login links are not a weaker form of authentication. For our use case, they're stronger. They eliminate entire categories of attacks (credential stuffing, phishing, password spraying) while being simpler for both users and developers.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="takeaways-from-version-2">Takeaways from Version 2<a href="https://curling.io/blog/passwordless-auth-done-right#takeaways-from-version-2" class="hash-link" aria-label="Direct link to Takeaways from Version 2" title="Direct link to Takeaways from Version 2">​</a></h2>
<p>Version 2's login link flow is straightforward: enter your email, get a code, enter the code, you're in. It works. But after a decade and hundreds of thousands of users, a few pain points emerged.</p>
<p><strong>One email, one identity.</strong> In Version 2, your email <em>is</em> your identity. Log in with a different email, you get a different account. This has been a real source of confusion. A club manager registers with their work email, then tries to log in from their phone with their personal email, and they're looking at an empty account. We'd get support requests about "missing registrations" that were really just the same person with two accounts.</p>
<p><strong>Unhelpful error messages.</strong> This one came up a lot. A curler would request a login link, wait a minute or two, not see the email, and request another one. The first email was usually just delayed. Many email clients only check for new mail every 5 to 15 minutes. When the first email finally arrived and they clicked it, they'd get a generic "invalid token" error because the second request had replaced the first. They'd wonder if they mistyped something, try again, get confused, and contact support.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="version-3-adds-multiple-email-addresses">Version 3 Adds Multiple Email Addresses<a href="https://curling.io/blog/passwordless-auth-done-right#version-3-adds-multiple-email-addresses" class="hash-link" aria-label="Direct link to Version 3 Adds Multiple Email Addresses" title="Direct link to Version 3 Adds Multiple Email Addresses">​</a></h2>
<p>This is the big addition. Users can now associate multiple verified email addresses with their account:</p>
<p><strong>Add an email.</strong> From the account page, enter a new email address. We send a verification code using the same mechanism as login.</p>
<p><strong>Verify it.</strong> Enter the code. The email is now linked to your account and marked as verified. Unverified emails can't be used for anything.</p>
<p><strong>Set it as primary.</strong> Any verified email can be promoted to primary. Your primary email is what shows up across the platform, in admin dashboards, on team rosters, in email communications. Changing your primary is a single click.</p>
<p><strong>Remove it.</strong> Non-primary emails can be removed. You can't remove your primary (switch to a different one first) and you can't remove your last verified email (you need at least one to log in).</p>
<p><strong>Log in with any of them.</strong> This is the key benefit. Once you've added and verified multiple emails, logging in with <em>any</em> of them resolves to the same account, the same registrations, the same history. No more duplicate accounts because you used a different email.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="how-it-works-under-the-hood">How It Works Under the Hood<a href="https://curling.io/blog/passwordless-auth-done-right#how-it-works-under-the-hood" class="hash-link" aria-label="Direct link to How It Works Under the Hood" title="Direct link to How It Works Under the Hood">​</a></h3>
<p>Each email row tracks a few key fields:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">email               -- globally unique, one owner max</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">user_id             -- nullable; not set until verified for first-time logins</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">token_hash          -- pending verification token</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">previous_token_hash -- the token that was replaced (for superseded detection)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">verified_at         -- null until verified</span><br></span></code></pre></div></div>
<p>This single structure handles both login verification and email ownership.</p>
<p>The design means multi-email support was almost free. The same verification flow used for login handles email verification for logged-in users. The only difference is that the user is already set when you're adding an email to your existing account.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="superseded-token-detection">Superseded Token Detection<a href="https://curling.io/blog/passwordless-auth-done-right#superseded-token-detection" class="hash-link" aria-label="Direct link to Superseded Token Detection" title="Direct link to Superseded Token Detection">​</a></h2>
<p>Remember the delayed email problem from Version 2? A curler requests a link, doesn't see it right away, requests another, and then clicks the first one when it finally arrives. Version 3 handles this gracefully.</p>
<p>When a new login link is requested, we don't just overwrite the old token. We move the current <code>token_hash</code> to <code>previous_token_hash</code> first. If someone then tries the old token, we can tell the difference:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub type AuthError {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  InvalidToken      // not found or already used</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  SupersededToken   // replaced by a newer token</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  ExpiredToken      // past expiry</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  UserDisabled      // account disabled</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  DatabaseError(sqlight.Error)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>A <code>SupersededToken</code> gets a specific message: "This code has been replaced. Check for a more recent email or request a new link." Instead of leaving users confused, we point them in the right direction.</p>
<p>This is a pattern we use throughout Version 3. Gleam's union types let us model every possible error state explicitly, and the compiler ensures we handle all of them. No forgotten edge cases, no generic catch-all error messages.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="oauth-google-and-facebook">OAuth (Google and Facebook)<a href="https://curling.io/blog/passwordless-auth-done-right#oauth-google-and-facebook" class="hash-link" aria-label="Direct link to OAuth (Google and Facebook)" title="Direct link to OAuth (Google and Facebook)">​</a></h2>
<p>Login links aren't the only way in. Google and Facebook login work alongside them for users who prefer one-click SSO.</p>
<p>The OAuth flow uses a central auth subdomain (<code>auth.curling.io</code>) to handle callbacks, since both providers require a fixed redirect URI. After the provider verifies the user's identity, we look up the email in the same table used for login links. If the email exists with a user, log them in. If not, create the user and a verified email row (OAuth emails are pre-verified by the provider).</p>
<p>The same user resolution logic, regardless of how you authenticate.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="a-note-on-rolling-your-own-auth">A Note on Rolling Your Own Auth<a href="https://curling.io/blog/passwordless-auth-done-right#a-note-on-rolling-your-own-auth" class="hash-link" aria-label="Direct link to A Note on Rolling Your Own Auth" title="Direct link to A Note on Rolling Your Own Auth">​</a></h2>
<p>Writing your own authentication is generally a bad idea. Battle-tested libraries like <a href="https://github.com/heartcombo/devise" target="_blank">Devise</a> (Ruby), <a href="https://next-auth.js.org/" target="_blank">NextAuth</a> (JavaScript), and <a href="https://django-allauth.readthedocs.io/" target="_blank">django-allauth</a> (Python) exist for good reason. They've been hardened over years of real-world use and security audits. If you're building on a stack that has a mature auth library, use it.</p>
<p>We couldn't find an existing Gleam auth library that was the right fit for our specific needs, but we didn't design in a vacuum. We studied Devise's modules extensively (Lockable, Timeoutable, Trackable, Confirmable) and used them as a checklist for what a production auth system needs to handle. Every security decision we made, from constant-time comparison to layered rate limiting to email enumeration prevention, was informed by what these libraries have learned the hard way over the past decade.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="whats-next">What's Next<a href="https://curling.io/blog/passwordless-auth-done-right#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next">​</a></h2>
<p>Authentication is foundational, but the real value of multi-email support shows up when it connects to everything else: registrations, team management, admin permissions, payment history. As we build those features, having clean user resolution across multiple emails will simplify a lot of workflows that were painful in Version 2.</p>
<p>We're also considering optional two-factor authentication for club administrators, via authenticator apps or SMS, for organizations that want to force an extra layer of security on accounts with administrative access.</p>
<hr>
<p><em>This is Part 3 of the Curling IO Foundation series. Next up: <a href="https://curling.io/blog/background-jobs-without-the-baggage">Background Jobs Without the Baggage</a>.</em></p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="foundation" term="foundation"/>
        <category label="authentication" term="authentication"/>
        <category label="gleam" term="gleam"/>
        <category label="security" term="security"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[Bilingual by Design]]></title>
        <id>https://curling.io/blog/bilingual-by-design</id>
        <link href="https://curling.io/blog/bilingual-by-design"/>
        <updated>2026-02-15T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Curling IO serves hundreds of clubs across Canada, where English and French aren't optional, they're official languages. A club in Quebec needs a fully French experience. A national organization like Curling Canada needs both. Rails has mature i18n support and Version 2 has been fully bilingual from the start, but after a decade of maintaining around 10,000 YAML translation keys, we've hit the limits of what that approach can catch: missing keys, missing translations, and unused keys that accumulate silently over time.]]></summary>
        <content type="html"><![CDATA[<p>Curling IO serves hundreds of clubs across Canada, where English and French aren't optional, they're official languages. A club in Quebec needs a fully French experience. A national organization like Curling Canada needs both. Rails has mature i18n support and Version 2 has been fully bilingual from the start, but after a decade of maintaining around 10,000 YAML translation keys, we've hit the limits of what that approach can catch: missing keys, missing translations, and unused keys that accumulate silently over time.</p>
<p>In Version 3, we wanted compile-time guarantees that make those problems impossible. This post covers how we designed the i18n system, why we split it into two layers, and what we changed from Version 2.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="two-kinds-of-text-two-different-systems">Two Kinds of Text, Two Different Systems<a href="https://curling.io/blog/bilingual-by-design#two-kinds-of-text-two-different-systems" class="hash-link" aria-label="Direct link to Two Kinds of Text, Two Different Systems" title="Direct link to Two Kinds of Text, Two Different Systems">​</a></h2>
<p>Every bilingual web application has two fundamentally different kinds of text:</p>
<ol>
<li><strong>App labels</strong>: static UI strings like "Leagues", "Contact", "Email". These are written by developers and change at deploy time.</li>
<li><strong>Content</strong>: user-entered data like league names, summaries, and descriptions. These are entered by club managers and may change at any time.</li>
</ol>
<table><thead><tr><th></th><th>App Labels</th><th>Content</th></tr></thead><tbody><tr><td><strong>Who writes it</strong></td><td>Developers</td><td>Club managers</td></tr><tr><td><strong>When it changes</strong></td><td>At deploy time</td><td>At any time</td></tr><tr><td><strong>Where it lives</strong></td><td>Compiled code</td><td>Database</td></tr><tr><td><strong>Scope</strong></td><td>Global (all clubs)</td><td>Per-record</td></tr></tbody></table>
<p>In Version 2, app labels came from Rails i18n YAML files loaded at boot, and content was stored in per-language database columns (<code>name_en</code>, <code>name_fr</code>, <code>summary_en</code>, <code>summary_fr</code>). This worked, but had pain points we wanted to address.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="what-version-2-taught-us">What Version 2 Taught Us<a href="https://curling.io/blog/bilingual-by-design#what-version-2-taught-us" class="hash-link" aria-label="Direct link to What Version 2 Taught Us" title="Direct link to What Version 2 Taught Us">​</a></h2>
<p><strong>The YAML files grew unwieldy.</strong> Over the years, our <code>en.yml</code> and <code>fr.yml</code> files accumulated thousands of keys each. Finding unused keys was a manual process. Ensuring every English key had a French counterpart required discipline that occasionally slipped, resulting in untranslated UI elements showing up in production as blank strings or key paths like <code>en.registrations.confirm_button</code>.</p>
<p><strong>Per-language columns didn't scale.</strong> The <code>leagues</code> table in Version 2 has <code>name_en</code>, <code>name_fr</code>, <code>summary_en</code>, <code>summary_fr</code>, <code>description_en</code>, <code>description_fr</code>, <code>post_registration_message_en</code>, <code>post_registration_message_fr</code>, <code>required_reading_en</code>, <code>required_reading_fr</code>. That's 10 columns just for translated text on one table. Every new translatable field requires a migration adding two columns. If we ever added a third language, it would mean another column for every translatable field on every table.</p>
<p><strong>Blank vs. missing was ambiguous.</strong> When <code>name_fr</code> was empty, did the manager intentionally leave it blank, or did they just not get around to translating it? The system couldn't tell the difference.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="version-3-app-label-translations">Version 3: App Label Translations<a href="https://curling.io/blog/bilingual-by-design#version-3-app-label-translations" class="hash-link" aria-label="Direct link to Version 3: App Label Translations" title="Direct link to Version 3: App Label Translations">​</a></h2>
<p>App labels are compiled directly into the application. No database, no YAML files, no runtime file loading. Just Gleam code.</p>
<p>The structure is simple:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">translations/keys.gleam   constants for every key</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">translations/en.gleam     English values</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">translations/fr.gleam     French values</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">translations.gleam        builds the lookup, provides t()</span><br></span></code></pre></div></div>
<p><strong>Key constants</strong> are <code>pub const</code> values in <code>keys.gleam</code>. This is extra code we've added specifically to get compile-time correctness. Every key used in a page or language file references these constants, so a misspelled key is a compile error rather than a silent runtime bug:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub const leagues = "leagues"</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub const contact = "contact"</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub const email_label = "email_label"</span><br></span></code></pre></div></div>
<p>Notice these are flat, no nesting. In Version 2, Rails i18n keys were nested by namespace (<code>en.registrations.confirm_button</code>, <code>en.leagues.index.title</code>, etc.). In practice, the nesting added complexity without much benefit: keys were harder to grep for, reorganizing namespaces meant touching multiple files, and deeply nested YAML was error-prone to edit. Flat keys are simpler to search, simpler to maintain, and work just as well with thousands of entries.</p>
<p><strong>Language files</strong> map those constants to translated values:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">// en.gleam</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">#(k.leagues, "Leagues"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">#(k.contact, "Contact"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">#(k.email_label, "Email"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">// fr.gleam</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">#(k.leagues, "Ligues"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">#(k.contact, "Contact"),</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">#(k.email_label, "Courriel"),</span><br></span></code></pre></div></div>
<p>At startup, <code>translations.build()</code> assembles these into a nested dictionary (outer key is language code, inner key is translation key). This dictionary gets stored in the application context and threaded into every request. Lookup is two hash table reads, effectively instant.</p>
<p><strong>In pages</strong>, we partial-apply the lookup function for the current language:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">let t = t.t(rc.translations, rc.lang, _)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">// Then just call t() with key constants</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">element.text(t(k.leagues))   // "Leagues" or "Ligues"</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">element.text(t(k.contact))  // "Contact" (same in both languages!)</span><br></span></code></pre></div></div>
<p>This is clean, type-safe, and impossible to mess up at runtime. If a key is missing from a language file, the UI shows <code>"[missing: postal_code_label]"</code> so it's immediately clear something needs to be fixed. Much better than a silent blank.</p>
<h3 class="anchor anchorWithStickyNavbar_LWe7" id="catching-problems-at-compile-and-test-time">Catching Problems at Compile and Test Time<a href="https://curling.io/blog/bilingual-by-design#catching-problems-at-compile-and-test-time" class="hash-link" aria-label="Direct link to Catching Problems at Compile and Test Time" title="Direct link to Catching Problems at Compile and Test Time">​</a></h3>
<p>Using constants instead of raw strings means a typo like <code>t(k.legues)</code> is a compile error because the constant doesn't exist. That alone eliminates an entire class of bugs we dealt with in Version 2.</p>
<p>We also have a test that verifies every English key has a French counterpart and vice versa:</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">pub fn should_have_same_keys_in_en_and_fr_test() {</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let en_keys = en.all() |&gt; dict.keys() |&gt; set.from_list()</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  let fr_keys = fr.all() |&gt; dict.keys() |&gt; set.from_list()</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  set.difference(en_keys, fr_keys) |&gt; should.equal(set.new())</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">  set.difference(fr_keys, en_keys) |&gt; should.equal(set.new())</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">}</span><br></span></code></pre></div></div>
<p>Adding a key to English without adding it to French fails the test suite. No more discovering untranslated strings in production.</p>
<p>We also have a script that checks for keys defined but never actually used in any page or template (dead key detection). Between the compiler, the parity test, and the unused key checker, translation problems are caught long before they reach users.</p>
<p>Notice how small the test surface is. Because Gleam's type system handles so much at compile time, our tests focus on validating design decisions and business logic rather than guarding against runtime surprises. In Version 2 with Rails, a lot of test effort goes toward catching things that a type system would prevent: nil values, wrong argument types, missing method calls. In Gleam, the compiler handles all of that, so our translation tests are purely about the logic we actually care about: do all languages have the same keys? Does the fallback behavior work correctly?</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="version-3-content-translations">Version 3: Content Translations<a href="https://curling.io/blog/bilingual-by-design#version-3-content-translations" class="hash-link" aria-label="Direct link to Version 3: Content Translations" title="Direct link to Version 3: Content Translations">​</a></h2>
<p>Content translations (the user-entered data) take a different approach. Instead of per-language columns, each translatable table has a single <code>translations</code> JSON column:</p>
<div class="language-json codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-json codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token punctuation" style="color:rgb(199, 146, 234)">{</span><span class="token property">"fr"</span><span class="token operator" style="color:rgb(137, 221, 255)">:</span><span class="token plain"> </span><span class="token punctuation" style="color:rgb(199, 146, 234)">{</span><span class="token property">"name"</span><span class="token operator" style="color:rgb(137, 221, 255)">:</span><span class="token plain"> </span><span class="token string" style="color:rgb(195, 232, 141)">"Ligue du lundi"</span><span class="token punctuation" style="color:rgb(199, 146, 234)">,</span><span class="token plain"> </span><span class="token property">"summary"</span><span class="token operator" style="color:rgb(137, 221, 255)">:</span><span class="token plain"> </span><span class="token string" style="color:rgb(195, 232, 141)">"Résumé en français"</span><span class="token punctuation" style="color:rgb(199, 146, 234)">}</span><span class="token punctuation" style="color:rgb(199, 146, 234)">}</span><br></span></code></pre></div></div>
<p>The primary language value lives in the regular column (<code>name</code>, <code>summary</code>). Only secondary language overrides go in the JSON. This means:</p>
<ul>
<li><strong>Primary language reads are free.</strong> No JSON parsing needed, just read the column.</li>
<li><strong>No translations? No overhead.</strong> The column is <code>NULL</code>.</li>
<li><strong>Adding translatable fields requires zero schema changes.</strong> Just start putting the field name in the JSON.</li>
<li><strong>Adding a third language is just another key in the JSON.</strong> No migration needed.</li>
</ul>
<p>The resolution logic is straightforward: if the user is viewing in the primary language, return the regular column. If they're viewing in a secondary language, look it up in the JSON. If it's not there, fall back to the primary language value. Every field degrades gracefully. A missing French translation shows the English text rather than nothing.</p>
<div class="language-gleam codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#bfc7d5;--prism-background-color:#292d3e"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-gleam codeBlock_bY9V thin-scrollbar" style="color:#bfc7d5;background-color:#292d3e"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#bfc7d5"><span class="token plain">// Parse once per record</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">let parsed = i18n.parse(league.data.translations)</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">// Resolve each field for the current language</span><br></span><span class="token-line" style="color:#bfc7d5"><span class="token plain">let name = i18n.resolve(league.data.name, parsed, lang, primary_lang, "name")</span><br></span></code></pre></div></div>
<p>This solves the blank-vs-missing ambiguity from Version 2: if a field isn't in the <code>translations</code> JSON, it simply hasn't been translated yet, and the primary language value is shown. No guessing.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="language-routing">Language Routing<a href="https://curling.io/blog/bilingual-by-design#language-routing" class="hash-link" aria-label="Direct link to Language Routing" title="Direct link to Language Routing">​</a></h2>
<p>Language selection comes from the URL path, not cookies or browser headers. This keeps things deterministic and shareable. A French URL always shows French content:</p>
<ul>
<li><code>/leagues</code> - primary language (no prefix)</li>
<li><code>/fr/leagues</code> - French (secondary language prefix)</li>
<li><code>/en/leagues</code> - redirects to <code>/leagues</code> (primary language never gets a prefix)</li>
</ul>
<p>The router extracts the language from the first path segment, stores it in the request context, and every page and API handler downstream uses it. Clean, predictable, and great for SEO.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="whats-next">What's Next<a href="https://curling.io/blog/bilingual-by-design#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next">​</a></h2>
<p>The i18n foundation is in place. As we build out more features (admin interfaces, registration flows, email notifications) we'll add keys to the app label system and the <code>translations</code> JSON column to more tables. The system is designed to scale to thousands of keys without any architectural changes.</p>
<hr>
<p><em>This is Part 2 of the Curling IO Foundation series. Next up: <a href="https://curling.io/blog/passwordless-auth-done-right">passwordless authentication and multi-email support</a>.</em></p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="foundation" term="foundation"/>
        <category label="i18n" term="i18n"/>
        <category label="gleam" term="gleam"/>
        <category label="architecture" term="architecture"/>
    </entry>
    <entry>
        <title type="html"><![CDATA[The Next Version of Curling IO]]></title>
        <id>https://curling.io/blog/the-next-version-of-curling-io</id>
        <link href="https://curling.io/blog/the-next-version-of-curling-io"/>
        <updated>2026-02-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[We're building the next version of Curling IO - same features, same experience, stronger foundation. After over a decade of serving hundreds of curling organizations across Canada, the US, and internationally, we're investing in technical upgrades that set Curling IO up for the next 20+ years.]]></summary>
        <content type="html"><![CDATA[<p>We're building the next version of Curling IO - same features, same experience, stronger foundation. After over a decade of serving hundreds of curling organizations across Canada, the US, and internationally, we're investing in technical upgrades that set Curling IO up for the next 20+ years.</p>
<p>This is Part 1 of a series of technical posts documenting our journey and decisions as we build.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="how-this-affects-you">How This Affects You<a href="https://curling.io/blog/the-next-version-of-curling-io#how-this-affects-you" class="hash-link" aria-label="Direct link to How This Affects You" title="Direct link to How This Affects You">​</a></h2>
<p><strong>For club managers:</strong> Curling IO isn't changing from your perspective. We're upgrading the technical infrastructure behind the scenes - no downtime, no disruption, no action required from you. Your data stays exactly where it is. When the upgrade is complete (thoroughly tested and proven), the transition will be seamless. You'll have the same Curling IO you know, even faster and more reliable.</p>
<p><strong>For curlers:</strong> You won't notice the technology change. Registration, scoring, schedules, results - everything works the same way. You might notice things feel snappier, especially during peak registration times or when checking live scores at major competitions.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="why-a-new-foundation">Why a New Foundation?<a href="https://curling.io/blog/the-next-version-of-curling-io#why-a-new-foundation" class="hash-link" aria-label="Direct link to Why a New Foundation?" title="Direct link to Why a New Foundation?">​</a></h2>
<p>Curling IO has been serving curling clubs since late 2014. We upgraded the platform once already in 2019, and our Version 2 system has been running successfully on Rails ever since. Everything works, clubs are happy, and growth is strong. So why invest in a new foundation?</p>
<p>Because we're planning to be here for the long haul. This new foundation ensures Curling IO can serve curling clubs not just for the next few years, but for the next 20+ years. While competitors come and go, we're investing in infrastructure built to last and evolve with the sport.</p>
<p><strong>Three key reasons:</strong></p>
<ol>
<li>
<p><strong>AI Agent APIs</strong> - We're adding new APIs specifically designed for AI agents to work with easily. Your existing web interface isn't changing - these are additional capabilities on top of what you already use. As AI assistants become ubiquitous (whether you love them or not), we're designing for that reality. Users are already asking Claude.ai and Google's Gemini to help them register for leagues or check schedules - we want those interactions to work reliably.</p>
</li>
<li>
<p><strong>Concurrency and correctness</strong> - As the platform grows, we need stronger guarantees around correctness and performance. We want a tech stack that can handle massive concurrency - both during peak registration periods and during large traffic spikes for scoring and results at provincial and national competitions like the Scotties and Brier. It needs to prevent runtime errors and scale efficiently as the sport grows.</p>
</li>
<li>
<p><strong>Easier developer onboarding</strong> - We want a codebase that's easy to hand off between developers. Type safety and functional patterns make code self-documenting - new developers (with help from AI coding assistants) can jump in and contribute safely without deep institutional knowledge. Gleam's deterministic patterns are much easier for AI assistants to reason about than dynamic languages, which means faster onboarding and more productive collaboration between developers and their AI tools. This significantly reduces key-person dependency.</p>
</li>
</ol>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="choosing-the-tech-stack">Choosing the Tech Stack<a href="https://curling.io/blog/the-next-version-of-curling-io#choosing-the-tech-stack" class="hash-link" aria-label="Direct link to Choosing the Tech Stack" title="Direct link to Choosing the Tech Stack">​</a></h2>
<p>Given our goals - agent-native APIs, clean architecture, and battle-tested reliability at scale - we need a stack that can deliver:</p>
<p><strong>What we're looking for</strong></p>
<ul>
<li><strong>Type safety</strong> - Catch errors at compile time, not in production</li>
<li><strong>Massive concurrency</strong> - Handle thousands of simultaneous registrations</li>
<li><strong>Functional patterns</strong> - Predictable, testable code that developers can reason about</li>
<li><strong>Shared types</strong> - Same data structures on client and server</li>
<li><strong>Proven at scale</strong> - Used in production by major companies</li>
</ul>
<p><strong>Why Gleam?</strong></p>
<p><a href="https://gleam.run/" target="_blank">Gleam</a> checks every box. It's a type-safe functional language that compiles to both Erlang (for the server) and JavaScript (for the client). You get:</p>
<ul>
<li><strong><a href="https://www.erlang.org/blog/a-brief-beam-primer/" target="_blank">BEAM VM</a> reliability</strong> - The same runtime that powers WhatsApp (billions of messages with ~50 engineers) and Discord (millions of concurrent users). Built-in fault tolerance and lightweight processes.</li>
<li><strong>ML-family type system</strong> - Stronger than Elixir, catches entire classes of bugs at compile time</li>
<li><strong><a href="https://lustre.build/" target="_blank">Lustre</a> for frontend</strong> - An <a href="https://elm-lang.org/" target="_blank">Elm</a>-architecture framework for Gleam that compiles to JavaScript. We already love Elm for our current frontend apps, and Lustre brings that same architecture everywhere. Same types from database to UI, all type-safe.</li>
<li><strong>Simple language</strong> - Focused, consistent, easy to reason about. Perfect for building deterministic APIs.</li>
</ul>
<p><strong>What about other options?</strong></p>
<p>We didn't jump straight to Gleam. We seriously considered several other stacks:</p>
<p><strong><a href="https://postgrest.org/" target="_blank">PostgREST</a> + Elm/Lustre</strong> - This was a very serious contender using tools we're already familiar with. PostgREST provides instant APIs from your PostgreSQL schema, and Elm/Lustre provides type-safe UI. But we'd need something else for background jobs (like Go), and the tooling for building, debugging, and testing PostgreSQL functions isn't great. The multi-language setup felt fragmented.</p>
<p><strong><a href="https://safe-stack.github.io/" target="_blank">F# SAFE Stack</a></strong> - F# is a fantastic functional language with a solid .NET ecosystem. The SAFE stack (Suave/Saturn, Azure/AWS, Fable, Elmish) offers full-stack type safety with The Elm Architecture (TEA) on the frontend. We really liked this option, but ultimately chose Gleam for its BEAM VM benefits and simpler deployment story.</p>
<p><strong>TypeScript + Node + React</strong> - The obvious choice given its massive ecosystem and the sheer volume of material AI agents can draw from. We've been using React off and on since 2014 and like Redux (which is Elm-inspired) for the frontend. But JavaScript/TypeScript has issues we couldn't get past: nulls everywhere, a type system that feels bolted on (TypeScript's safety is opt-out with <code>any</code>, <code>as</code>, and <code>!</code>), relentless library churn, and recurring npm supply chain vulnerabilities. React itself has become overly complex. Popularity is a real advantage, but it wasn't enough to outweigh the correctness and simplicity we wanted.</p>
<p><strong>BEAM was the killer feature.</strong> None of these other options came close to what the BEAM VM offers: rock-solid concurrency, fault tolerance, and proven scalability (WhatsApp, Discord, etc.). Gleam gives us BEAM on the backend with familiar functional patterns (like Elm) on the frontend, all in a single language.</p>
<p><strong>Why not stick with Rails?</strong></p>
<p>To be clear: we love <a href="https://rubyonrails.org/" target="_blank">Ruby on Rails</a>. It's been amazing for rapid development and has scaled well for our needs. For heavier interactive features, we've already been using Elm for frontend apps like Bracket Builder, Scheduling, Scoring, and Results widgets, with PostgREST providing clean APIs on the backend. This functional programming approach has worked really well.</p>
<p>But for where we're headed, we need:</p>
<ul>
<li>Compile-time safety (Ruby is dynamically typed)</li>
<li>BEAM-level concurrency (Rails doesn't even come close)</li>
<li>Shared client/server types (Rails is backend-only)</li>
<li>Functional patterns built-in (Rails is object-oriented)</li>
</ul>
<p>This isn't about Rails being bad - we've been building with it since version 1.2 and nothing else lets you iterate that fast. But over time we've moved towards functional, compiled, typed languages because runtime errors are the absolute worst to debug and fix. Gleam gives us that correctness without giving up the developer experience we love about Rails.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="sqlite-over-postgresql">SQLite Over PostgreSQL<a href="https://curling.io/blog/the-next-version-of-curling-io#sqlite-over-postgresql" class="hash-link" aria-label="Direct link to SQLite Over PostgreSQL" title="Direct link to SQLite Over PostgreSQL">​</a></h2>
<p><strong>This one surprised us.</strong></p>
<p>Curling IO Version 2 runs on PostgreSQL. We assumed we'd use Postgres for Version 3. But we're betting on <a href="https://sqlite.org/" target="_blank">SQLite</a> instead.</p>
<p><strong>Why SQLite?</strong></p>
<ul>
<li><strong>Isolated databases</strong> - Clean separation of concerns with dedicated database files</li>
<li><strong>In-process speed</strong> - Zero network latency between app and database</li>
<li><strong>Operational simplicity</strong> - No database server to manage, tune, or cluster</li>
<li><strong>Cost savings</strong> - Orders of magnitude cheaper than <a href="https://www.crunchydata.com/products/crunchy-bridge" target="_blank">managed Postgres</a></li>
<li><strong><a href="https://litestream.io/" target="_blank">Litestream</a> backups</strong> - Continuous replication to offsite storage</li>
</ul>
<p><strong>What about scale?</strong></p>
<p>For the same infrastructure budget as Rails + PostgreSQL, we expect Gleam + SQLite to handle roughly:</p>
<ul>
<li>1,000x more concurrent connections</li>
<li>100x the data volume</li>
<li>100x the throughput during peak traffic (registrations, live scoring)</li>
</ul>
<p>These numbers sound outrageous, but they aren't pulled out of thin air. BEAM processes are ~2KB each vs Rails threads at ~1MB - that's where the 1,000x connection multiplier comes from. And eliminating network round-trips to a separate database server (SQLite runs in-process) while leveraging BEAM's lightweight concurrency model accounts for the throughput gains. These are well-documented characteristics of the technology, not optimistic guesses.</p>
<p>That said, we'll be validating everything with extensive benchmarking and real-world load tests as we build.</p>
<p>If SQLite doesn't scale as expected, we'll just stick with PostgreSQL - an equally great database.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="vertical-first-scaling">Vertical-First Scaling<a href="https://curling.io/blog/the-next-version-of-curling-io#vertical-first-scaling" class="hash-link" aria-label="Direct link to Vertical-First Scaling" title="Direct link to Vertical-First Scaling">​</a></h2>
<p>Our scaling strategy is simple: start on a single powerful server and scale up from there.</p>
<ul>
<li>One server with co-located app + database (one thing to deploy, backup, monitor)</li>
<li>Warm backup server + continuous offsite backups</li>
<li>Scale up CPU and RAM as we grow</li>
<li>No distributed systems complexity until actually needed</li>
</ul>
<p>If we ever outgrow a single server, we have a clean path to splitting things up. But with BEAM's concurrency and SQLite's in-process speed, we expect that to be a long way off.</p>
<h2 class="anchor anchorWithStickyNavbar_LWe7" id="whats-next">What's Next<a href="https://curling.io/blog/the-next-version-of-curling-io#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next">​</a></h2>
<p>We'll be building this out over the coming months alongside the existing platform. Curling IO Version 2 isn't going anywhere - it continues to run and receive updates as usual. When Version 3 is ready and thoroughly tested, we'll transition everyone over at once - a clean, coordinated switch rather than a drawn-out process. We won't make that switch until we're confident everything is solid.</p>
<hr>
<p><em>This is Part 1 of the Curling IO Foundation series. Next up: <a href="https://curling.io/blog/bilingual-by-design">bilingual support with compile-time guarantees</a>.</em></p>]]></content>
        <author>
            <name>Dave Rapin</name>
        </author>
        <category label="foundation" term="foundation"/>
        <category label="architecture" term="architecture"/>
        <category label="gleam" term="gleam"/>
        <category label="sqlite" term="sqlite"/>
        <category label="scaling" term="scaling"/>
    </entry>
</feed>